{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "profile-draft.schema.json",
  "title": "ProfileDraft",
  "description": "Single JSON contract emitted by extract(); rendered deterministically by render().",
  "type": "object",
  "additionalProperties": false,
  "required": ["source", "confidence", "company", "voice", "icp", "competitors", "pillars", "products", "brand", "gaps"],
  "properties": {
    "source": {
      "type": "object",
      "additionalProperties": false,
      "required": ["type", "value"],
      "properties": {
        "type": {"type": "string", "enum": ["url", "file", "text"]},
        "value": {"type": "string", "minLength": 1},
        "crawled_pages": {"type": "integer", "minimum": 0}
      }
    },
    "confidence": {"type": "string", "enum": ["high", "medium", "low"]},
    "company": {
      "type": "object",
      "additionalProperties": false,
      "required": ["name", "slug", "brand_name", "description", "markets", "social_handle"],
      "properties": {
        "name": {"type": "string", "minLength": 1},
        "slug": {
          "type": "string",
          "pattern": "^[a-z0-9]([a-z0-9-]{0,38}[a-z0-9])?$",
          "description": "Kebab-case profile slug. 1-40 chars. Output of slugify()."
        },
        "brand_name": {"type": "string", "minLength": 1},
        "description": {"type": "string", "minLength": 10},
        "markets": {"type": "array", "items": {"type": "string"}, "minItems": 1},
        "social_handle": {"type": "string"}
      }
    },
    "voice": {
      "type": "object",
      "additionalProperties": false,
      "required": ["tone", "principles", "ban_list", "examples"],
      "properties": {
        "tone": {"type": "string", "minLength": 5},
        "principles": {
          "type": "array",
          "items": {"type": "string"},
          "minItems": 3,
          "description": "At least 3 specific voice principles."
        },
        "ban_list": {"type": "array", "items": {"type": "string"}},
        "examples": {"type": "array", "items": {"type": "string"}}
      }
    },
    "icp": {
      "type": "object",
      "additionalProperties": false,
      "required": ["personas", "verticals", "company_size"],
      "properties": {
        "personas": {
          "type": "array",
          "minItems": 1,
          "items": {
            "type": "object",
            "additionalProperties": false,
            "required": ["title", "pain_points", "goals"],
            "properties": {
              "title": {"type": "string", "minLength": 1},
              "pain_points": {"type": "array", "items": {"type": "string"}, "minItems": 1},
              "goals": {"type": "array", "items": {"type": "string"}, "minItems": 1}
            }
          }
        },
        "verticals": {"type": "array", "items": {"type": "string"}},
        "company_size": {"type": "string"}
      }
    },
    "competitors": {
      "type": "array",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["name", "differentiator"],
        "properties": {
          "name": {"type": "string", "minLength": 1},
          "differentiator": {"type": "string", "minLength": 5},
          "bucket": {
            "type": "string",
            "description": "Optional competitive category this rival belongs to, e.g. 'Direct rivals', 'Adjacent tech', 'Incumbents', 'Status-quo / DIY'. Lets render() group competitors into tiers instead of one flat list. Free text — use the buckets that actually describe this market."
          }
        }
      }
    },
    "pillars": {
      "type": "array",
      "items": {"type": "string"},
      "minItems": 2,
      "description": "Content pillars — at least 2."
    },
    "products": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["name", "slug", "description", "capabilities", "use_cases", "references"],
        "properties": {
          "name": {"type": "string", "minLength": 1},
          "slug": {"type": "string", "pattern": "^[a-z0-9]([a-z0-9-]{0,38}[a-z0-9])?$"},
          "flagship": {"type": "boolean"},
          "description": {"type": "string", "minLength": 10},
          "technical_notes": {"type": "string"},
          "capabilities": {"type": "array", "items": {"type": "string"}},
          "use_cases": {"type": "array", "items": {"type": "string"}, "minItems": 1},
          "source_pages": {"type": "array", "items": {"type": "string"}},
          "references": {
            "type": "array",
            "items": {
              "type": "object",
              "additionalProperties": false,
              "required": ["url", "title"],
              "properties": {
                "url": {"type": "string", "minLength": 1},
                "title": {"type": "string", "minLength": 1},
                "summary": {"type": "string"}
              }
            }
          }
        }
      }
    },
    "brand": {
      "type": "object",
      "additionalProperties": false,
      "required": ["palette"],
      "properties": {
        "palette": {
          "type": "array",
          "items": {"type": "string"},
          "minItems": 1,
          "description": "Hex colour codes sampled from the website (best-effort)."
        },
        "assets_note": {"type": "string"}
      }
    },
    "buyer_journey": {
      "type": "object",
      "additionalProperties": false,
      "description": "Optional. The 'why now, where in the journey, and what moves them' layer — derived from the personas and whatever industry/market context is in the source. Everything here is inferred (the real answers only the seller knows), so each derived item should also surface in operator_confirm[] for the review gate. Absent = render() ships a persona-derived skeleton the operator fills in.",
      "properties": {
        "primary_persona": {
          "type": "string",
          "description": "The persona title whose full journey the `stages` array describes — normally the primary economic buyer. Should match one of icp.personas[].title."
        },
        "triggers": {
          "type": "array",
          "description": "Trigger events ('why now') that time an outreach touch, roughly ranked by how strongly each predicts a buy.",
          "items": {
            "type": "object",
            "additionalProperties": false,
            "required": ["event", "activates", "predicts"],
            "properties": {
              "event": {"type": "string", "minLength": 1, "description": "The observable event, e.g. 'New aircraft type induction / large fleet order'."},
              "activates": {"type": "string", "description": "The persona this trigger most activates (a persona title)."},
              "predicts": {"type": "string", "description": "Why this event predicts a buy — the opening it creates."},
              "detect_via": {"type": "string", "description": "How to detect it, free web first then a connected feed, e.g. 'OEM order press, careers page → RocketReach hiring signal'."}
            }
          }
        },
        "stages": {
          "type": "array",
          "description": "The buyer journey for the primary_persona — one row per stage (e.g. Unaware → Problem-aware → Solution-aware → Vendor-aware → Evaluation → Decision → Expansion).",
          "items": {
            "type": "object",
            "additionalProperties": false,
            "required": ["stage", "buyer_question"],
            "properties": {
              "stage": {"type": "string", "minLength": 1, "description": "Stage name, e.g. 'Problem-aware'."},
              "buyer_question": {"type": "string", "description": "The question in the buyer's head at this stage."},
              "angle": {"type": "string", "description": "The content angle that moves them forward."},
              "proof": {"type": "string", "description": "The proof they need to advance."},
              "objection": {"type": "string", "description": "The objection to pre-empt at this stage."},
              "pillar": {"type": "string", "description": "The content pillar / asset that serves this stage — ideally one of pillars[]."}
            }
          }
        },
        "persona_deltas": {
          "type": "array",
          "description": "For secondary personas, only where their journey differs from the primary — where they enter, what leads, their sharpest trigger and objection.",
          "items": {
            "type": "object",
            "additionalProperties": false,
            "required": ["persona"],
            "properties": {
              "persona": {"type": "string", "minLength": 1, "description": "Persona title (matches an icp.personas[].title)."},
              "notes": {"type": "string", "description": "How this persona's journey differs — entry stage, leading message, sharpest trigger, top objection."}
            }
          }
        },
        "operator_confirm": {
          "type": "array",
          "items": {"type": "string"},
          "description": "Inferred items only the seller can truly confirm — real sales-cycle length, which trigger historically closes, who signs vs. who champions, the objections actually heard in the room. Surfaced at the review gate."
        }
      }
    },
    "settings": {
      "type": "object",
      "additionalProperties": false,
      "description": "Operator-supplied config from the interview. Optional; render() supplies defaults when absent.",
      "properties": {
        "title": {"type": "string"},
        "email_signature": {"type": "string"},
        "segment_mix": {"type": "string", "description": "e.g. '70% startup / 30% enterprise'"},
        "monthly_tool_budget_usd": {"type": "number", "minimum": 0},
        "per_run_cap_usd": {"type": "number", "minimum": 0},
        "language": {"type": "string"},
        "home_base": {"type": "string"},
        "content_root": {"type": "string"},
        "connectors": {
          "type": "array",
          "description": "Per-connector plan/entitlement facts, one entry per metered tool the operator connects. Generic across tools (Vibe, RocketReach, Firecrawl, ...) — not tool-specific. Distinct from the 'Tools connected' status-only block: this captures WHICH plan and HOW MUCH it allows, so a flat-subscription tool (cost_usd near 0 per call) can still be paced against a real monthly limit instead of only the dollar cap.",
          "items": {
            "type": "object",
            "additionalProperties": false,
            "required": ["name"],
            "properties": {
              "name": {
                "type": "string",
                "minLength": 1,
                "description": "Tool identifier, e.g. 'rocketreach', 'apollo', 'vibe_prospecting', 'firecrawl'."
              },
              "plan": {
                "type": "string",
                "description": "Plan/tier name as the vendor names it, e.g. 'Ultimate + Phone', 'Boost pack'."
              },
              "billing": {
                "type": "string",
                "enum": ["subscription", "credit_pack", "pay_per_use", "free"],
                "description": "How the plan is billed — governs whether the dollar cap or the monthly_allowance count is the real constraint."
              },
              "monthly_allowance": {
                "type": "object",
                "additionalProperties": false,
                "description": "The plan's real recurring limit, in the unit that actually matters for this tool (not always dollars).",
                "properties": {
                  "unit": {"type": "string", "description": "e.g. 'premium_lookups', 'credits', 'exports'."},
                  "limit": {"type": "number", "minimum": 0}
                }
              },
              "features": {
                "type": "array",
                "items": {"type": "string"},
                "description": "Premium features this plan enables, e.g. 'intent', 'phone', 'api_access', 'signal_search'. A skill should gate a premium-only facet/call on its presence here rather than assuming it works."
              },
              "notes": {
                "type": "string",
                "description": "Free text for anything uncertain — mark CONFIRM if the operator wasn't sure of an exact number."
              }
            }
          }
        }
      }
    },
    "gaps": {
      "type": "array",
      "items": {"type": "string"},
      "description": "Fields the brain couldn't fill from the source. Empty = no gaps."
    }
  }
}
