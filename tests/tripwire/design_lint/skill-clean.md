# Fictional skill body — the SD11 off-state

The mirror of `skill-contradiction.md`: the same two guardrails, stated so they agree. A
rule with no committed quiet case cannot be told apart from a rule that fires on nothing.

## Step 1 — write the design

Never ship a version log with a design revision.

## Step 2 — revise the design

A revision carries no version log: the design is replaced in place and the date moves.
