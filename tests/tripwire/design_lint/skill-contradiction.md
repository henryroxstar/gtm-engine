# Fictional skill body — the SD11 negative control

This file is not a shipped skill. It exists so the contradictory-guardrail rule has a
committed input it must fire on, and so a version of it that stops firing is named.

## Step 1 — write the design

Never ship a version log with a design revision.

## Step 2 — revise the design

A revision must append a version log as its final section.
