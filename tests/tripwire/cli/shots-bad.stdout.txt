{
  "path": "shots-bad.shots.json",
  "ok": false,
  "errors": [
    "style_scaffold.provider_model is missing or empty",
    "style_scaffold.look uses negation phrasing ('no') \u2014 prompt fields are positive-only; move exclusions to style_scaffold.negative as plain nouns, or state the constraint positively in `stability`",
    "synthetic_disclosure is present but empty \u2014 declare the disclosure line the finished render will carry, or omit the key entirely. A present-but-blank declaration reads as an attempt to open the presenter engine without accepting the Art. 50 duty that opens it.",
    "shot[1] duration_s must be a number > 0",
    "shot[1] role 'narrator' is not one of ['broll', 'presenter', 'screen']",
    "shot[1] stacks 3 camera moves (zoom, pan, dolly) in one shot \u2014 one move per shot; split the beat instead (documented warped-geometry failure mode on every current video model)",
    "shot[1] motion_prompt merely restates `camera` ('dolly in while zooming and panning left') \u2014 camera is how the LENS moves, motion_prompt is what the SUBJECT does; a restatement leaves the subject with nothing to do",
    "shot[1].visual asks the video model to render in-frame text ('sign that reads'). Diffusion video models paint shapes that resemble text rather than typesetting it, which is where the garbled frames came from \u2014 this is a model limitation, not a prompt-quality problem. Describe the scene without the text and let gtm_core.captions burn it in at finish time.",
    "shot[1] declares `audio_bed: 'silent'` with no reason. Deliberate silence is a real device and accidental silence is a defect; only the reason tells them apart. Say what the beat is doing.",
    "shot[1].lighting uses negation phrasing ('no') \u2014 prompt fields are positive-only; move exclusions to style_scaffold.negative as plain nouns, or state the constraint positively in `stability`",
    "shot[2] has a spoken line and role='presenter' but nothing in motion_prompt/expression says the subject is speaking \u2014 image-to-video models animate what the prompt describes, and a gesture-only direction renders a closed mouth through the whole line (verified 2026-08-19, mouth shut in 9 of 10 frames). Add the speech to motion_prompt, e.g. 'he speaks directly to camera, articulating each word, and ...'.",
    "shot[2] is a speaking presenter shot and no engine can serve it. engine 'heygen_avatar' may only render DISCLOSED synthetic media, and this caller did not declare disclosure for the 'presenter' role. This is the W4 panel gate, kept where it actually bites: the pre-registered panel asks whether judges who know the operator can tell the avatar from real footage, so it governs the case where the viewer is NOT told. A render that carries the tenant's [disclosure].line is not making that claim and is not gated on it; an undisclosed one is, and stays shut until the panel in gtm_core/panel_eval.py returns PASS. Three ways forward: declare disclosure (--disclosed, or `synthetic_disclosure` on the shot list); shoot REAL FOOTAGE, which carries no disclosure duty at all; or use the FACELESS format \u2014 b-roll plus held soul_2 stills, voice-over and captions.",
    "shot[2] has a spoken line but voice_grade='instant' \u2014 a shipped VO requires a PROFESSIONAL voice clone (30 min-3 h of clean audio). An instant clone does not train a custom model at all; it guesses from prior training data, which is why it does not sound like the speaker. Record the grade with: python -m gtm_core.brandkit --profile <p> --set identity.voice_grade --value professional --note '<when/how cloned>'",
    "shot[2] has a `spoken` line but the brand kit records no `identity.voice_id` and this shot list declares no `vo_source` \u2014 nothing will voice it, so the line renders as silence and only `video_lint` V10 catches it, after every shot has been paid for. Two ways out: clone a voice (`identity-kit`, which owns the Art. 50 consent gate) and record it as identity.voice_id; or make this a CAPTIONS-ONLY cut \u2014 move the line to the shot's `caption`, clear `spoken`, and give it an `audio_bed`. A captions-and-music cut is a legitimate deliverable, not a downgrade; silence nobody chose is not. If the VO comes from somewhere else entirely, name it in top-level `vo_source`.",
    "shot[3] motion_prompt contains 'finger': fingers raised, held up or counted \u2014 the engine renders a counted hand shape. An avatar engine renders the metaphor literally and hands the presenter a prop \u2014 counted fingers (2026-08-29) and a wooden board (2026-08-30) both shipped this way. Describe what the PERSON does ('hands resting still', 'one open palm toward camera') and let the graphics carry the count, the layers and the comparison",
    "shot[3] duration_s=2 is shorter than its own spoken line (20 words \u2248 7.25s at 2.76 words/sec, the slow end of the measured band) \u2014 the VO will overrun the shot, forcing the mux to truncate or time-stretch, which is exactly what reads as broken lip sync. Round up to 8s, or cut words.",
    "shot[3] is a speaking presenter shot and no engine can serve it. engine 'heygen_avatar' may only render DISCLOSED synthetic media, and this caller did not declare disclosure for the 'presenter' role. This is the W4 panel gate, kept where it actually bites: the pre-registered panel asks whether judges who know the operator can tell the avatar from real footage, so it governs the case where the viewer is NOT told. A render that carries the tenant's [disclosure].line is not making that claim and is not gated on it; an undisclosed one is, and stays shut until the panel in gtm_core/panel_eval.py returns PASS. Three ways forward: declare disclosure (--disclosed, or `synthetic_disclosure` on the shot list); shoot REAL FOOTAGE, which carries no disclosure duty at all; or use the FACELESS format \u2014 b-roll plus held soul_2 stills, voice-over and captions.",
    "shot[3] has a spoken line but voice_grade='instant' \u2014 a shipped VO requires a PROFESSIONAL voice clone (30 min-3 h of clean audio). An instant clone does not train a custom model at all; it guesses from prior training data, which is why it does not sound like the speaker. Record the grade with: python -m gtm_core.brandkit --profile <p> --set identity.voice_grade --value professional --note '<when/how cloned>'",
    "shot[3] has a `spoken` line but the brand kit records no `identity.voice_id` and this shot list declares no `vo_source` \u2014 nothing will voice it, so the line renders as silence and only `video_lint` V10 catches it, after every shot has been paid for. Two ways out: clone a voice (`identity-kit`, which owns the Art. 50 consent gate) and record it as identity.voice_id; or make this a CAPTIONS-ONLY cut \u2014 move the line to the shot's `caption`, clear `spoken`, and give it an `audio_bed`. A captions-and-music cut is a legitimate deliverable, not a downgrade; silence nobody chose is not. If the VO comes from somewhere else entirely, name it in top-level `vo_source`.",
    "shot[4] missing required field `visual`",
    "shot[4] has a spoken line but voice_grade='instant' \u2014 a shipped VO requires a PROFESSIONAL voice clone (30 min-3 h of clean audio). An instant clone does not train a custom model at all; it guesses from prior training data, which is why it does not sound like the speaker. Record the grade with: python -m gtm_core.brandkit --profile <p> --set identity.voice_grade --value professional --note '<when/how cloned>'",
    "shot[4] has a `spoken` line but the brand kit records no `identity.voice_id` and this shot list declares no `vo_source` \u2014 nothing will voice it, so the line renders as silence and only `video_lint` V10 catches it, after every shot has been paid for. Two ways out: clone a voice (`identity-kit`, which owns the Art. 50 consent gate) and record it as identity.voice_id; or make this a CAPTIONS-ONLY cut \u2014 move the line to the shot's `caption`, clear `spoken`, and give it an `audio_bed`. A captions-and-music cut is a legitimate deliverable, not a downgrade; silence nobody chose is not. If the VO comes from somewhere else entirely, name it in top-level `vo_source`.",
    "shot[5] missing required field `camera`",
    "shot[5] duration_s=5 against a measured 3.00s VO leaves a 2.00s silent tail, past the 0.35s mux ceiling \u2014 the model spreads mouth motion across silence the audio never fills; render at duration=3s",
    "shot[5] is a speaking presenter shot and no engine can serve it. engine 'heygen_avatar' may only render DISCLOSED synthetic media, and this caller did not declare disclosure for the 'presenter' role. This is the W4 panel gate, kept where it actually bites: the pre-registered panel asks whether judges who know the operator can tell the avatar from real footage, so it governs the case where the viewer is NOT told. A render that carries the tenant's [disclosure].line is not making that claim and is not gated on it; an undisclosed one is, and stays shut until the panel in gtm_core/panel_eval.py returns PASS. Three ways forward: declare disclosure (--disclosed, or `synthetic_disclosure` on the shot list); shoot REAL FOOTAGE, which carries no disclosure duty at all; or use the FACELESS format \u2014 b-roll plus held soul_2 stills, voice-over and captions.",
    "shot[5] has a spoken line but voice_grade='instant' \u2014 a shipped VO requires a PROFESSIONAL voice clone (30 min-3 h of clean audio). An instant clone does not train a custom model at all; it guesses from prior training data, which is why it does not sound like the speaker. Record the grade with: python -m gtm_core.brandkit --profile <p> --set identity.voice_grade --value professional --note '<when/how cloned>'",
    "shot[5] has a `spoken` line but the brand kit records no `identity.voice_id` and this shot list declares no `vo_source` \u2014 nothing will voice it, so the line renders as silence and only `video_lint` V10 catches it, after every shot has been paid for. Two ways out: clone a voice (`identity-kit`, which owns the Art. 50 consent gate) and record it as identity.voice_id; or make this a CAPTIONS-ONLY cut \u2014 move the line to the shot's `caption`, clear `spoken`, and give it an `audio_bed`. A captions-and-music cut is a legitimate deliverable, not a downgrade; silence nobody chose is not. If the VO comes from somewhere else entirely, name it in top-level `vo_source`.",
    "shot[6] duration_s=4 against a measured 5.00s VO needs atempo=1.250 to fit, past the 1.15 ceiling \u2014 compressing the cloned voice that hard is audible; render at duration=5s",
    "shot[6] is a speaking presenter shot and no engine can serve it. engine 'heygen_avatar' may only render DISCLOSED synthetic media, and this caller did not declare disclosure for the 'presenter' role. This is the W4 panel gate, kept where it actually bites: the pre-registered panel asks whether judges who know the operator can tell the avatar from real footage, so it governs the case where the viewer is NOT told. A render that carries the tenant's [disclosure].line is not making that claim and is not gated on it; an undisclosed one is, and stays shut until the panel in gtm_core/panel_eval.py returns PASS. Three ways forward: declare disclosure (--disclosed, or `synthetic_disclosure` on the shot list); shoot REAL FOOTAGE, which carries no disclosure duty at all; or use the FACELESS format \u2014 b-roll plus held soul_2 stills, voice-over and captions.",
    "shot[6] has a spoken line but voice_grade='instant' \u2014 a shipped VO requires a PROFESSIONAL voice clone (30 min-3 h of clean audio). An instant clone does not train a custom model at all; it guesses from prior training data, which is why it does not sound like the speaker. Record the grade with: python -m gtm_core.brandkit --profile <p> --set identity.voice_grade --value professional --note '<when/how cloned>'",
    "shot[6] has a `spoken` line but the brand kit records no `identity.voice_id` and this shot list declares no `vo_source` \u2014 nothing will voice it, so the line renders as silence and only `video_lint` V10 catches it, after every shot has been paid for. Two ways out: clone a voice (`identity-kit`, which owns the Art. 50 consent gate) and record it as identity.voice_id; or make this a CAPTIONS-ONLY cut \u2014 move the line to the shot's `caption`, clear `spoken`, and give it an `audio_bed`. A captions-and-music cut is a legitimate deliverable, not a downgrade; silence nobody chose is not. If the VO comes from somewhere else entirely, name it in top-level `vo_source`.",
    "shot[7] missing required field `motion_prompt` \u2014 the model needs an ACTION, not just a start frame and a camera note, or it renders a person sitting still (documented 2026-08-18: 6/6 shots omitted this, V9 found 3/6 motionless)",
    "shot[7] has no `spoken` line and no `audio_bed` \u2014 a shot with neither is silent, and a run of them is what makes a finished film read as broken. Name the bed (room tone, a music cue, an SFX), or declare `audio_bed: \"silent \u2014 <why>\"` if the silence is the point.",
    "5 of 7 shots are role=presenter (71%) \u2014 ceiling is 60%. An asset that is almost entirely face shots reads as boring however good the script is; convert beats to role=screen (a capture of the actual thing) or role=broll (data, motion graphic, product surface)",
    "identity_bindings.look_a source is 608x1080 (portrait) but 16:9 needs landscape. Rendering it anyway fills the frame with a scaled copy of itself \u2014 a portrait look in a 16:9 frame leaves ~32% real picture. Pick a look whose native orientation matches (list_avatar_looks reports `preferred_orientation` and `image_width`/`image_height` per look), or compose that ratio as a deliberate layout and say so here."
  ],
  "warnings": [
    "shot[2] duration_s=20 exceeds every confirmed provider ceiling (15s \u2014 provider-duration-ceilings.md); the provider will clamp it and desync this file's stated timing",
    "shot[2] camera 'swoosh' matches no known move or framing term \u2014 prefer the controlled vocabulary (video-render/references/prompt-recipes.md)",
    "shot[2] motion_prompt 'waves' is 1 words \u2014 too thin to direct a render; name the subject's action, and what changes over the shot",
    "shot[2] duration_s=20 runs 18.25s past its spoken line (5 words \u2248 1.75s at 2.85 words/sec, the fast end of the measured band) \u2014 the model stretches mouth motion across the silence and the tail gets cut; trim the shot toward the line's real length",
    "shot[3] duration_s=2 is below 4s, the minimum seedance_2_0 accepts \u2014 the provider will clamp it up and the shot will run longer than this file claims",
    "shot[4] duration_s=4.5 is fractional \u2014 every current audio-capable video model takes an INTEGER duration and clamps anything else, desyncing this file's stated timing from what actually renders",
    "no shot has role=screen \u2014 nothing in this asset shows the thing being talked about. For a product or incident story, a real screen capture is the highest-value shot in the piece and the cheapest to produce",
    "total_duration_s=100 but the shots sum to 39.5s (drift 60.5s) \u2014 the stated timing and what renders will not match"
  ],
  "schema": "shots.schema.json",
  "schema_errors": [
    "$.style_scaffold: missing required 'provider_model'",
    "$.synthetic_disclosure: 0 chars < minLength 1",
    "$.shots[0].duration_s: expected type number, got str",
    "$.shots[0].role: 'narrator' not in enum ['presenter', 'broll', 'screen']",
    "$.shots[3]: missing required 'visual'",
    "$.shots[4]: missing required 'camera'",
    "$.shots[6]: missing required 'motion_prompt'"
  ]
}
