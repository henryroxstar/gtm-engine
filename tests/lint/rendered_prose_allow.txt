# Typed counts in rendered prose that are allowed to stay typed.
#
# One `<file>:<line>  # YYYY-MM-DD <why this number cannot go stale>` per line. The reason is
# the point: an entry without one is itself a failure, because the whole defect this gate
# exists for is a number nobody could tell was old. See tests/lint/rendered_prose_check.py.
#
# NOT the place for "I'll derive it later" — that is the state the gate was written to end.

gtm_core/email_campaign_dashboard/views_learn.py:209  # 2026-09-06 A closed fact about a PRIOR campaign ("the last attempt sent 24 emails, got one reply, and it was an unsubscribe"), used to say why this run changed its aim. It describes a run that has finished, so it cannot go stale the way a live count can — the same reason docstrings are exempt. Renders only on the profile-wide page.
# views_who.py:398's entry was removed 2026-09-11: the branch now renders `f['n']:,` in both
# arms instead of a typed `0` literal, so the exemption no longer describes a real finding.
