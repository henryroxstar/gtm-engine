# tests/lint/complexity_allowlist.txt — the §R10 file-length ratchet (docs/RULES.md).
#
# One entry per production file over 500 lines:  <repo-relative path> <ceiling>
# A listed file may shrink but never grow past its ceiling. When it shrinks, lower the
# ceiling in the same PR; when it drops to 500 or fewer lines, DELETE the entry (a stale
# entry fails the check). Raising a ceiling, or adding an entry, requires a dated reason
# on the line:  path 620  # 2026-09-02 <why this file must grow>  — the check compares
# against the committed list, so growth is a recorded decision, never a silent bump.
# Each raise needs its OWN reason: one written for an earlier raise does not cover a new one.
#
# Regenerate from scratch (drops every note — a re-freeze, not a routine edit):
#   uv run python tests/lint/complexity_check.py --freeze > tests/lint/complexity_allowlist.txt

agent/__main__.py 610  # 2026-09-15 +5 for client issue #245: the CLI gate-decision verb reads, discards, promotes and clears only THIS run's enroll draft (gate_draft with the run id) instead of the newest in the profile, which a leftover or concurrent run's draft could be. Prior 605: 2026-09-14 A11: new --gate-decision/--edited-content-file CLI flags plus _pack_gate_decision()/_dispatch_gate_successors() — the VPS/CLI twin of the backend's POST /v1/runs/{id}/gate, since a --pack run is a one-shot subprocess with no session/HTTP API for an operator to resolve a paused gate=true node from; also pushes a Telegram notification on pause (agent/gate_notify.py) so an unattended cron/timer run doesn't pause silently forever
agent/mcp/apollo/server.py 979
agent/mcp/rocketreach/server.py 578
agent/mcp/saleshandy/server.py 871  # 2026-09-15 +28 for client issue #244: private _list_sequences_page/_get_step_variants/_list_fields request functions (not MCP tools) so agent/email_dispatch.py can read the paused sequence back and refuse to enroll unless its copy matches the approved copy, and map approved field-label rows to Saleshandy field ids for the documented import shape. Prior 843: 2026-09-14 +89 for A11: split add_leads_to_sequence/import_prospects_to_sequence into internal _*_request() functions (accepting an explicit api_key) so agent/email_dispatch.py can call the real enrollment logic directly after an operator approves the pack graph's `sequence` gate, while the @mcp.tool() wrappers stay registered — denied to the brain outright by agent/permissions.py — for standalone-module/test compatibility. Prior 754 for 2026-09-03 +10 for the NOT_CONFIGURED sentinel and the comment explaining why 'connector absent' must stay distinguishable from 'API failed' — agent/optout_sweep.py compares against it by identity to stay quiet on the former while still exiting nonzero on the latter
backend/services/runs/pack_executor.py 658  # 2026-09-16 +18 for FL13/ST-06+RL-13: _execute_pack_run's top-of-function admission now reads this run's own runs.status once and skips the one-time _reserve_or_deny/start_run pair when it is already 'awaiting_approval' — a boot reconcile or a lease reclaim resuming a still-open gate wait, not a fresh start; re-running that admission on every resume incorrectly failed a run legitimately parked at a gate with a §R2 cap check that has no user action behind it, and re-wrote started_at, lying to every client polling/streaming it. Prior 640: 2026-09-16 +51 for FL12/RL-02: _budget_guard's predicate now also reads this run's own runs.status and returns False on 'canceled' (a third check alongside the existing acheck_budget/acheck_agent_budget AND), carries a parallel `cancelled` verdict list so a cancel-triggered stop is never mislabeled as a cost-cap failure, and _fail_from_outcome is a new helper (extracted from _execute_pack_run, which was pushing PLR0915's statement cap) that distinguishes the two before deciding whether to call _fail_run at all. Prior 589: 2026-09-15 lowered to 589 (M-07: the Gate-2 dispatch outcome classification moved to persistence.publish_refusal/enroll_refusal beside the run failure codes). Prior 590: 2026-09-15 +17 for client issues #244/#245: _gate_draft_content reads only this run's enroll draft and fails the run at the pause when it is missing or lacks the approved copy (the old newest-in-profile lookup tried plan drafts first and could show a leftover or another run's list); merged onto the 573 ceiling below, so 573 + 17. Prior 573: 2026-09-15 lowered to 573 (dispatch_target and gate kinds moved to gate_kinds.py). Prior 598: +10 for client issue #240: an approved enroll gate now promotes the bytes the operator was shown and clears only that draft — the profile lock is released at a gate, so re-reading "newest draft in the profile" could enroll another run's list. Prior 588: +21 for client issue #241 Q6: _promote_gate_draft fails the run when edited content reaches a gate with no draft to apply it to (backstop for POST /gate's refuses_edit, for a decision recorded before the durable gate row existed). Prior 567 for 2026-09-14 A11 follow-up: fix backend Gate-2 dispatch being structurally unreachable for email_enroll (approving `sequence` never enrolled — PENDING.md) — adds _dispatch_target (resolve a gated node's direct email_enroll successor, since a dispatch-declared node short-circuits to SKIPPED before it can pause itself) and _complete_approved_gate (flip both the approved node and a dispatched successor, mirroring the persistence/event calls for each)
agent/permissions.py 1126  # 2026-09-16 +70 (9 of them ruff-format line wrapping): _WRAPPER_PROGRAMS + wrapper-transparent _segment_program, so the deny floor classifies the program that actually RUNS rather than the first token — `env curl`, `xargs curl`, `timeout 5 curl` and `find -exec curl` were all ALLOWED while a bare `curl` was denied. Includes the `find -exec` walk and the bare-`env` dump case. The prose is load-bearing: the operand-skipping rule is why `env FOO=1 uv run pytest` stays allowed, and a future trim that drops it reintroduces false positives that train people to route around the floor  # 2026-09-14 +52 for A11: _SALESHANDY_ENROLL_LEAVES + _email_context/email_context()/in_email_context(), mirroring _REAP_PUBLISH_LEAVES/_publish_context exactly, so add_leads_to_sequence/import_prospects_to_sequence are denied to the brain everywhere except inside agent/email_dispatch.py's approved-enrollment dispatch window. Prior 1004 for 2026-09-11 +22 for U8 security fix stripping leading uv run flags before binary validation to prevent execution bypass
agent/publish.py 704
agent/session.py 546
cockpit/bot.py 539
cockpit/gates.py 600
gtm_core/account_integrity.py 1036  # 2026-09-10 +130: three enrollment-time refusal gates — `_refuse_parked_lanes` (a CSV row still carrying `lane` hold/excluded reaching `--require-verdict` enrollment: 52 excluded/already-enrolled + 14 hold rows measured at verdict=send on a live tenant pool with no --lane named), `_refuse_ambiguous_lane` (more than one enrollable lane in the CSV with no --lane to say which), and `_refuse_lane_state_mismatch` (the CSV's own `lane` column disagreeing with `lanes route`'s last recorded routing in evals/lanes-state.jsonl — a stale or hand-edited lane must not enrol on an old column's strength). Prior 1004 for 2026-09-09 +1: `LANE_VERDICTS` re-exported from the new gtm_core/lane_verdicts.py so gtm_core/prospects_consolidate can filter the hand-send list without an import cycle, and `--lane` choices now derive from LANE_VERDICTS keys instead of a hand-written list that had silently dropped `personalised`. Prior 1003 for 2026-09-03 +3: §R9 identity fictionalization (real academic-medical-centre and health-system examples reworded to generic phrasing and fictional domains) — the reword, not new logic, so the file stays the one filter implementation the dashboard and gate share
gtm_core/brandkit.py 501
gtm_core/build_eval_sheet.py 881
gtm_core/campaigns_dashboard.py 646  # 2026-09-04 +4: carry `roster_globs` through the campaign shaper. The shaper reshapes each manifest into a fixed key set, so a campaign declaring where its own accounts live had that declaration silently dropped — and a campaign-scoped page could then only count the shared prospect pool, rendering "731 people we will actually email of 859 loaded" for a run whose whole roster is 26 accounts
gtm_core/captions.py 1081  # 2026-09-11 +48: glyph colour derived from measured luminance (`_first_pole`) and `glyph_rgb` carried per screen into the sidecar — `palette.canvas` was assumed light, so a dark-theme kit drew every caption near-black over a dark picture and the contrast tier measured white instead of the type that was burned.
gtm_core/cells.py 740  # 2026-09-03 +139: lane on the cell map, lane/cell lookups by sequence, reply rate BY LANE with Wilson CI, and the cross-list overlap report — the oracle the lane design exists for
gtm_core/community_signal/render.py 663
gtm_core/content_quality/script.py 519  # 2026-09-11 Caption craft (K3): _caption_functions_check validates caption budget function column against sibling .shots.json.
gtm_core/creator_brief.py 617  # 2026-09-13 +10: allow writing briefs under tenant profile root within content root. Prior 607 for 2026-09-11 +96: Caption craft (K4): caption_voice optional decision, corroboration check against shotlist in check_against_shotlist(), markdown twin rendering. Prior 511 for 2026-09-07 the hook frame's face: `expression` reaches the markdown twin, and `check_against_shotlist` gains an absence-only finding when the brief decides a first-frame expression the shot list never received. Absence-only is the whole design — the two artifacts are written by different steps in different words, so comparing their text would refuse every legitimate rewording.
gtm_core/deck_consistency.py 502
gtm_core/email_compliance.py 909  # 2026-09-14 +51: reject region names (e.g. "Southeast Asia") in target_markets — a region never literally equals a lead's `country`, so left unguarded it either silently drops every real lead in it or, if ever auto-expanded, opens several jurisdictions at once with none of the per-country legal review the gate exists to force
gtm_core/eval_calibration.py 1773  # 2026-09-03 +121: `score` now persists its verdict beside the holdout and `is_calibrated` reads a recorded PASS instead of file existence — under the old rule the 2026-09-01 FAIL made the judge MORE trusted
gtm_core/gating.py 723  # 2026-09-16 +44: _public_doc_refs scans a stubbed skill's withheld files for docs/*.md citations that also ship publicly and threads them into render_stub_body, so a stub does not silently drop a citation a private-repo test (e.g. test_skills_reference_direct_response_patterns) expects to survive the carve. Prior 679 for 2026-09-11 +47: skill-manifest-rightsizing: extract/strip interface contract for stub_carve
gtm_core/gtm_distill.py 609
gtm_core/hook_score.py 708
gtm_core/hooks.py 863
gtm_core/knowledge_meta.py 552
gtm_core/metering.py 571  # 2026-09-16 +40: workspace_jsonl_month_total() plus the comment explaining it — the backend cap read only Postgres cost_records while every agent/mcp/* worker meters to the workspace costs.jsonl (stdio subprocess, no pool, no workspace id), so worker spend was invisible to a multi-tenant billing boundary. The two stores are disjoint, so the cap sums both; the prose is load-bearing because the disjointness is the assumption that makes summing correct rather than double-counting
gtm_core/outcomes.py 686  # 2026-09-16 +8: narrow unattributed()'s finish-*.json glob to finish-<digits>x<digits>.json so video_finish.execute's new finish-spec-<ratio>.json sidecar is never fed to load_finish (it isn't a FinishManifest, and raised an uncaught TypeError). Earlier reasons, 2026-09-11 +28: K8 _caption_attribution() reads score.json's TOP-LEVEL caption_mode/caption_voice/describe_share so the attribution row carries them as queryable tags. 2026-09-07 +25: _story_format() reads score.json's TOP-LEVEL story_format; 2026-09-03 +3: OPTOUT_OUTCOMES so the ledger knows who said stop
gtm_core/preflight_report.py 655
gtm_core/prospects_state.py 531  # 2026-09-10 +141: `LEDGER_STATUSES`/`RETIRED_STATUSES` plus `mark_replied()` — a positive reply only ever carries an email, so it is joined through `ready-to-load.csv` (the one file every consolidated row's email/company/domain/account_id lands in) to the ledger's own identity key, and never overrides a retired (disqualified/opt-out/closed-lost) account
gtm_core/render_manifest.py 700  # 2026-09-11 +7: `transitions` on FinishManifest — the non-cut joins the stitch applied, carried off the master's sidecar so a shot list's declared production.transition_in has evidence to be checked against at finish; nothing else in a finished file can say whether a declared dissolve happened. Prior 693 for 2026-09-11 +23: `captions_preburned` + `overlays` on FinishManifest and a kit-declared `captions_route` waiver — a per-shot burn stitched into a master finished with `captions: null`, which silently disarmed V3/V11 on the one film that needed them.
gtm_core/signal_record.py 645  # 2026-09-11 +5: signal record sync. Prior 645 for 2026-09-10 +6: `check_record` falls back to `signal_clause(why_now)` when the row carries no stored `signal_clause` — that column is derived, never one of ready-to-load.csv's 41 stored columns, so it was blank on every pooled row and the provenance check silently took the "nothing to source" exit on rows that in fact made a dated claim. Prior 639 for 2026-09-03 +4: JUDGE_COLUMNS gains judge_defect_class (the normalised routing key) plus the comment on why judge_verdict_reason cannot serve as one
gtm_core/spoiler_check.py 541
gtm_core/video_preflight.py 1048  # 2026-09-13 +12: detect and surface ffmpeg on PATH in preflight constraints, JSON serialization, and text report. Prior 1037 for 2026-09-11 +31: Two-Door classification engine (PRD 3 Axis 1) routing and --route CLI option. Prior 1006 for 2026-09-11 +56: Caption craft (K3) constraints.caption_mode derived (subtitles/narrative/mixed) and --lane CLI option. Prior 950 for 2026-09-07 StoryCapture + story_capture(): a story item's live_action capture default, derived from brief.protagonist and reported as a constraint. The reason sentence lives here rather than in the router body because the body's rule is to relay what this module resolves, never restate it.
gtm_core/vo_timings.py 524
gtm_core/voc/capture.py 576
gtm_core/voc/collect.py 653
gtm_core/voc/signals.py 510  # 2026-09-10 +88: `load_reporting`/`LoadReport`/`SignalReject` so `--validate` reports what FAILED instead of only a survivor count — a 76-record file with 6 errors read exactly like a clean 70-record one and exited 0 either way, so the six contradictions in it went unnoticed until each record was re-validated by hand.
plugin/skills/account-dossier/scripts/render_dossier_pydocx.py 729
plugin/skills/account-plan/scripts/render_account_plan_pydocx.py 642
tests/linter/content_linter.py 833
tests/linter/merge_render_linter.py 1869  # 2026-09-04 +2: register `seat-stakes-missing` and `offer-does-their-work` so the summary groups them like every other rule
tests/linter/outreach_pack_linter.py 2985  # 2026-09-10 +21: `_CEO_TITLE_CUES` and the explicit-title check at the head of `persona_of`. `_PERSONA_RULES` is first-match-wins with `ceo` LAST, so a compound title naming both an exec office and a function — "SVP, Chief Technology Officer" — matched the function cue first and seated as `cto`, and three `exec` spec rows failed the merge-render gate on `seat-stakes-missing` because the seat they resolved to was not the seat their copy argued. The promoted set is TITLES only (ceo/chief executive/chief operating/chief operations): `managing director` and `president` are seniority BANDS a functional chief also holds, and `founder`/`owner` are OWNERSHIP, so promoting either would move 33 "Co-Founder & CTO" rows — technical buyers — into the exec seat. Anti-cues still veto, so "Executive Vice President, Engineering" is unaffected. Prior 2964 for 2026-09-08 +119: word-bounded cue matching (`_cue_re`/`_matches`, `@cache`-compiled) — bare "cto" was a substring of "director", so 128 of 1,193 titles containing "director" seated as CTO, including a Director of Information Security (a CISO), a Managing Director (a CEO) and a Director of Talent Acquisition (nobody); plus a `founder-operator` persona (mapped to the `ceo` seat) so an SME owner-operator title resolves instead of falling into the unrecognised bucket, and `non_buyer_of`/`_NON_BUYER_CUES` so an Administrative or Executive Assistant is recognised as not a buyer rather than silently mis-seated. Prior 2845 for 2026-09-06 +38: a `[ROLE INBOX]` sentinel, so `greeting` stops demanding a name for a mailbox that has none. The sg-builders role-inbox pack wrote its persona as "unresolved - team inbox <address>", `_first_name` returned the literal word, and the rule asked for `Hi unresolved,` against a body that correctly said `Hi team,` - a rule whose only satisfying input is a wrong one. It is a sentinel and not prose-sniffing ("team inbox", "no named seat") for the same reason `[NAME UNRESOLVED]` is: nobody is on the other end is a claim the drafter should make on purpose, and a rule that reads prose is one any pack can talk its way past. The two states are NOT the same - `[NAME UNRESOLVED]` means a seat exists unfound and BLOCKS; `[ROLE INBOX]` means there is no seat and the pack is finished - so this is a different expected greeting, not a suppression, and a borrowed name in a role-inbox pack is still an error. Same-day `channel-order` keeps firing on a role-inbox pack carrying a DM and only changes its advice (drop the unusable section, not reorder the ladder), because exempting the case would leave a section nobody can send reported by nothing. Prior 2807 for 2026-09-06 +24: `parse_ladder` plus a widened `_TOUCH_RE`/`_SEQ_HEADER_RE`. Both regexes matched only the dash-separated ladder form (`**Touch 2 - Day 2 - email:**`), so every pack written in the equally correct paren form (`**Touch 2 (Day 2, email):**`) parsed as ZERO touches and both `touch-count` and `channel-order` were inert on it — six packs of one campaign, silently, and the widening immediately convicted one of them on channel-order. `parse_ladder` also exposes the per-touch DAY, which the campaign status page needs to say when a hand-sent 1:1 arc finishes; a second regex for that would be a second answer to how many emails a pack sends and over how long. Prior 2783 for # 2026-09-05 +132: `problem-asserts-internals` and `offer-not-a-solution-overview` — the operator stated both points three times and the copy never carried them, because voice.md rules 9 and 11 had no check. Rule 10 (agent as grammatical subject) quietly pulled every body back to asserting an architecture inferred from a website; both new rules convicted 6 of 6 packs before they passed them. Also narrows the 2026-09-04 external-anchor exemption, which had re-admitted the audit-shaped offer. Prior 2651 for 2026-09-04 +29: the `Would it be helpful if` offer shape (the sender retired both 'happy to' and 'I can'), giving verbs excluded in PAST tense too — 'would it be helpful if I SENT you the one-pager' was an artifact gate wearing the help-offer's clothes — and an external-anchor exemption on `offer-does-their-work`, because 'map your dispatch path against IMDA §2.1.2' is asymmetric knowledge while 'look at your dispatch path' is unpaid labour. Same day, 2622 for 2026-09-04 +83 for `capability-unargued`. `hook_coverage` has checked the SPREAD of capability declarations across a campaign since 2026-08-24 and never once checked that a body argues its own: three of six packs declared identity / protocol-proxy / security-policy and all three argued OBSERVABILITY — 'can the buyer check the record afterwards'. Evidence language is about the past, so the batch read as audit logging rather than runtime governance, which is what the operator meant by 'not even sure it has anything to do with agentic runtime governance'. The rule fails a body carrying none of its declared group's vocabulary, and a body that names its group once while spending the paragraph on evidence. Same day, 2539 for 2026-09-04 +79 for the two rules that close the gap between voice.md and what actually shipped. `seat-stakes-missing`: the persona-axis table has said since 2026-07 that a founder loses sleep over the stalled deal, not attribution — and `persona-lead-mismatch` could never enforce it, because it is fail-open by construction (it fires only on BORROWED vocabulary, and a mechanism-altitude body borrows nothing). Six emails to founders led on attribution mechanics and passed every gate. `offer-does-their-work`: an offer to go and look inside their own build is worth less than an hour of their own engineer and presumes they had not looked; the asymmetry we hold is cross-market. Same day, 2460 for 2026-09-04 +6 for the `I can <verb>` offer shape: "happy to" was retired at the sender's instruction, and without this the only CTA frames the gate accepted were the interrogative offer he had already rejected and the one he had just banned. Same day, 2454 for 2026-09-04 +19: the gate was MANDATING the phrasing the operator rejected as AI slop. `hedge-missing` accepted "tell me if this is already handled" and nothing plainer, so rotating that one sentence four ways to dodge repetition was the compliant path; `cta-question` required a question mark, forcing the interrogative-offer frame that ran in 6 of 6 rejected drafts; and the sign-off strip took one line, so a "Regards" above the name became the body's last sentence and failed every body that signed off the way he actually does. Earlier the same day, 2435 for 2026-09-04 +31 for `_is_person_name`: the old one-line `_NAME_RE` had NO SPACE in it, so every romanised Chinese, Malay or Indian given name written as two tokens — Hui Jie, Wei Ming, Siti Nurhaliza — was rejected with "is not a person's name". On a Singapore campaign that is not an edge case; the rule refused the market the run targeted. The replacement checks the placeholder vocabulary PER TOKEN so "name unconfirmed" is still refused whole. Earlier the same day, 2404 for 2026-09-04 +18 so a pack can be JUDGED: `_PACK_HEADING_RE` accepts both authored heading shapes (the old single-shape regex yielded company "" on all six packs of the sg-builders campaign — missing judge context reported as an error by nothing), and `**Contact:**` makes the sendable address a field instead of prose in the notes, which is the adjudication record's join key. Earlier the same day, 2400 for credit-is-verdict + cta-omits-gap + the help-offer exemption: three rules voice.md had stated in prose since 2026-08-19 with nothing enforcing them; all five packs of the sg-builders campaign passed every gate at zero errors while grading the reader in the opener
tests/linter/partnership_brief_linter.py 540
gtm_core/video_finish/cli.py 604  # 2026-09-16 +2: `_cmd_run` passes `spec=spec` to `execute(...)` so the finish-spec-<ratio>.json replay sidecar gets the spec that was actually given. Earlier reasons, 2026-09-11 +10: `--transitions-from-shots` on the stitch verb — the CLI path that turns a shot list's per-shot production.transition_in into stitch's per-join list, so a film with one dissolve and one fade no longer has to dissolve every join to honour either. Prior 592 for 2026-09-11 +91: `overlays` and `overlay-scene` subcommands — the shot list's `production.overlay` had no consumer anywhere, so four chat bubbles a film specified were never drawn and nothing reported them missing.
gtm_core/video_finish/plan.py 576  # 2026-09-11 +32: TRANSITIONS_KIND and `_sibling_transitions` — the master's `<stem>.transitions.json` ingested onto the finish plan (and so the manifest) verbatim, the same carry-forward the pre-burned caption/overlay sidecars get, and folded into the plan_id so a sidecar appearing after a run cannot be short-circuited. Prior 544 for 2026-09-11 +167 (first listing): pre-burned captions/overlays sidecar discovery beside the source and box scaling to the finished frame (`_preburned_from_source`, `_scaled_to_frame`), so the finish manifest carries the geometry the lint measures instead of declaring none.
gtm_core/screen_ui/scenes/chat_bubble.py 515  # 2026-09-11 first listing: the one scene that composites a message bubble beside a phone on a provider render; carries its own fit-or-refuse (names the smallest w_frac that fits) and glyph refusal so an emoji cannot ship as a tofu box.
gtm_core/screen_ui/cli.py 521  # 2026-09-16 first listing: `glittery-sleeping-yao` changes #3/#4 — `render_scene()` extracted so the rebuild driver can call it per shot without re-invoking `main([...])` (which re-reads the kit file and prints on every call), the build-record refusal/warn/delete-before-render wiring around it, and the new `fit-layout` pseudo-scene's three flags (`--for-scene`/`--caption-lines`/`--margin-px`) dispatching to `screen_ui/fit_layout.py`.
