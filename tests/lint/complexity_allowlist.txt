# tests/lint/complexity_allowlist.txt — the §R10 file-length ratchet (docs/RULES.md).
#
# One entry per production file over 500 lines:  <repo-relative path> <ceiling>
# A listed file may shrink but never grow past its ceiling. When it shrinks, lower the
# ceiling in the same PR; when it drops to 500 or fewer lines, DELETE the entry (a stale
# entry fails the check). Raising a ceiling, or adding an entry, requires a dated reason
# on the line:  path 620  # 2026-09-02 <why this file must grow>  — the check compares
# against the committed list, so growth is a recorded decision, never a silent bump.
# Each raise needs its OWN reason: one written for an earlier raise does not cover a new one.
#
# Regenerate from scratch (drops every note — a re-freeze, not a routine edit):
#   uv run python tests/lint/complexity_check.py --freeze > tests/lint/complexity_allowlist.txt

agent/__main__.py 624  # 2026-09-21 +14 for the 2026-09-21 sequencer capability contract (SC9/SC2): `dnc_add` is the engine's THIRD external effect, so the CLI gate-decision verb grew a third dispatch branch beside publish and email_enroll — it resolves which effect a gate's successors declare, reads/discards this run's own dnc-draft, and dispatches through agent/dnc_dispatch.py inside the narrow dnc_context() window. It cannot share the enroll branch: the two route to different Python dispatchers, and a generic truthy check is exactly how an approved enrollment would be sent through the wrong one. Prior 610: 2026-09-21 +16 for SC9: `dnc_add` is the engine's THIRD external effect, so the CLI gate-decision verb grew a third dispatch branch beside publish and email_enroll — it resolves which effect a gate's successors declare, reads/discards this run's own dnc-draft, and dispatches through agent/dnc_dispatch.py inside the narrow dnc_context() window. The branch cannot be shared with the enroll one: the two route to different Python dispatchers, and a generic truthy check is exactly how an approved enrollment would be sent through the wrong dispatcher. Prior 610: 2026-09-15 +5 for client issue #245: the CLI gate-decision verb reads, discards, promotes and clears only THIS run's enroll draft (gate_draft with the run id) instead of the newest in the profile, which a leftover or concurrent run's draft could be. Prior 605: 2026-09-14 A11: new --gate-decision/--edited-content-file CLI flags plus _pack_gate_decision()/_dispatch_gate_successors() — the VPS/CLI twin of the backend's POST /v1/runs/{id}/gate, since a --pack run is a one-shot subprocess with no session/HTTP API for an operator to resolve a paused gate=true node from; also pushes a Telegram notification on pause (agent/gate_notify.py) so an unattended cron/timer run doesn't pause silently forever
agent/mcp/apollo/server.py 979
agent/mcp/rocketreach/server.py 582  # 2026-09-21 +4: route HTTP through resilience.py (429 retry + Retry-After + circuit breaker) and name a rate-limit refusal instead of reporting it as a generic error. The policy itself is a SPLIT into agent/mcp/rocketreach/resilience.py (the §R10-preferred remedy); these 4 lines are only the wiring. Prior 578
agent/mcp/saleshandy/server.py 930  # 2026-09-21 +44 for the 2026-09-21 sequencer capability contract (SC9/SC2): two READ-ONLY tools (get_unibox_categories, get_outcomes) and an optional category_ids filter on get_inbox_threads. No write verb was added — tests/agent/test_saleshandy_wrapper.py AST-asserts the tool set is the old one plus exactly these two, and that no tool name matches a reply/send/DNC-write verb. Prior 2026-09-21 +17 for the unified-inbox endpoint correction: the original GET /unified-inbox/threads and /threads/{id} were never hit live and 404'd on first contact with a real account, which is why a wave's three opt-outs were unreadable. Both are now POST /unified-inbox/emails and GET /unified-inbox/emails/{id}, with filters in the JSON body and `items` (not `threads`) as the list key. The added lines are the CORRECTED comments recording the source, the incident, and — deliberately — what is STILL unverified (`isRead`'s 0/1 encoding has no caller today, and the per-message field names are thinner in the doc than the list endpoint's), so the next session does not read a corrected path as a fully-confirmed surface. Prior 871:  # 2026-09-15 +28 for client issue #244: private _list_sequences_page/_get_step_variants/_list_fields request functions (not MCP tools) so agent/email_dispatch.py can read the paused sequence back and refuse to enroll unless its copy matches the approved copy, and map approved field-label rows to Saleshandy field ids for the documented import shape. Prior 843: 2026-09-14 +89 for A11: split add_leads_to_sequence/import_prospects_to_sequence into internal _*_request() functions (accepting an explicit api_key) so agent/email_dispatch.py can call the real enrollment logic directly after an operator approves the pack graph's `sequence` gate, while the @mcp.tool() wrappers stay registered — denied to the brain outright by agent/permissions.py — for standalone-module/test compatibility. Prior 754 for 2026-09-03 +10 for the NOT_CONFIGURED sentinel and the comment explaining why 'connector absent' must stay distinguishable from 'API failed' — agent/optout_sweep.py compares against it by identity to stay quiet on the former while still exiting nonzero on the latter
agent/permissions.py 1319  # 2026-09-25 Task 4.2 (+63): deny mail and chat send verbs on every connector. Prior 1256: 2026-09-25 Task 4.2 (+15): deny mail and chat send verbs on every connector. Prior 1241: 2026-09-21 +79 for the 2026-09-21 sequencer capability contract (SC9/SC2): a third dispatch context (dnc_context) with its own leaf set, plus the five DNC REMOVAL verbs denied with no admitting context at all. This file is a register of denials, so a new irreversible effect necessarily lengthens it; the removal verbs are named even though the in-repo connector exposes none, because 'we did not build one' is not a guarantee against a hosted connector that does. Prior 2026-09-20 +5: Refine category permissions fail-closed logic per PRD §4.3. Prior 1157: 2026-09-20 +31: Refactor MCP tool categorization and ruff format
agent/publish.py 700  # 2026-09-22 +12: second-span field-promotion fix — the comment recording why EVERY ⟦POST⟧ span is excised (a forged empty ⟦IDENTITY⟧ in a later span zeroed identity_used and retired the Art. 50 duty), plus the corrected unknown-token-retention docstring. Prior 688
agent/session.py 547  # 2026-09-25 Task 1.1 (+1): GTM_RUNTIME=headless env override

backend/routers/runs.py 581  # 2026-09-25 Task 4.5 (+11): next_step field on ErrorDetail in error responses. Prior 570: 2026-09-20 +26 for validate_disclosure gate check on edit decision. Prior 544
backend/routers/webhooks.py 660  # 2026-09-20 +31 for Saleshandy conversationId/timestamp dedup, thread_id and replied event check (Issue #283). Prior 629: 2026-09-20 +1 for webhook exact-match denylist fix
backend/schemas.py 560  # 2026-09-26 (+2): GateRequest edited_content validation on non-edit decisions. Prior 558: 2026-09-25 Task 4.5 (+2): schema hardening and next_step field. Prior 556: 2026-09-25 Task 4.5 (+1): next_step field on ErrorDetail. Prior 555: 2026-09-20 +3: Additional fields
backend/services/runs/pack_executor.py 798  # 2026-09-25 (+11): check_gate2_preview Gate 2 preview check in _gate_draft_content for enroll gates. Prior 787: 2026-09-21 +46 for the 2026-09-21 sequencer capability contract (SC9/SC2): the dnc_add dispatch branch beside email_enroll, and the dnc draft kind threaded through gate resolution. Parallel to the enroll branch by necessity — the two route to different dispatchers and a generic check cannot pick one. Prior 2026-09-20 +5 for inbound-triage strict ⟦GATE:reply⟧ requirement (Issue #283). Prior 738: 2026-09-20 +1 for aborting start_run on canceled runs. Prior 737: 2026-09-20 +13 for reply draft extraction at gate to avoid plan fallback. Prior 723: 2026-09-17 +42 for client backend readiness (RL-01): publish successor dispatch, asset draft discovery, and _complete_approved_gate successor outcome block. Prior 681: 2026-09-17 +22 for Fleet Phase C: _apply_pack_inputs_and_context for §R5 context injection into node runs. Prior 659: 2026-09-16 +1 for FL9/ST-09: _on_node's node event now carries `error` when the node just failed with one (previously {run_id, node_id, state} only — a client watching the stream couldn't attribute a run-level error to its node). Prior 658: 2026-09-16 +18 for FL13/ST-06+RL-13: _execute_pack_run's top-of-function admission now reads this run's own runs.status once and skips the one-time _reserve_or_deny/start_run pair when it is already 'awaiting_approval' — a boot reconcile or a lease reclaim resuming a still-open gate wait, not a fresh start; re-running that admission on every resume incorrectly failed a run legitimately parked at a gate with a §R2 cap check that has no user action behind it, and re-wrote started_at, lying to every client polling/streaming it. Prior 640: 2026-09-16 +51 for FL12/RL-02: _budget_guard's predicate now also reads this run's own runs.status and returns False on 'canceled' (a third check alongside the existing acheck_budget/acheck_agent_budget AND), carries a parallel `cancelled` verdict list so a cancel-triggered stop is never mislabeled as a cost-cap failure, and _fail_from_outcome is a new helper (extracted from _execute_pack_run, which was pushing PLR0915's statement cap) that distinguishes the two before deciding whether to call _fail_run at all. Prior 589: 2026-09-15 lowered to 589 (M-07: the Gate-2 dispatch outcome classification moved to persistence.publish_refusal/enroll_refusal beside the run failure codes). Prior 590: 2026-09-15 +17 for client issues #244/#245: _gate_draft_content reads only this run's enroll draft and fails the run at the pause when it is missing or lacks the approved copy (the old newest-in-profile lookup tried plan drafts first and could show a leftover or another run's list); merged onto the 573 ceiling below, so 573 + 17. Prior 573: 2026-09-15 lowered to 573 (dispatch_target and gate kinds moved to gate_kinds.py). Prior 598: +10 for client issue #240: an approved enroll gate now promotes the bytes the operator was shown and clears only that draft — the profile lock is released at a gate, so re-reading "newest draft in the profile" could enroll another run's list. Prior 588: +21 for client issue #241 Q6: _promote_gate_draft fails the run when edited content reaches a gate with no draft to apply it to (backstop for POST /gate's refuses_edit, for a decision recorded before the durable gate row existed). Prior 567 for 2026-09-14 A11 follow-up: fix backend Gate-2 dispatch being structurally unreachable for email_enroll (approving `sequence` never enrolled — PENDING.md) — adds _dispatch_target (resolve a gated node's direct email_enroll successor, since a dispatch-declared node short-circuits to SKIPPED before it can pause itself) and _complete_approved_gate (flip both the approved node and a dispatched successor, mirroring the persistence/event calls for each)
cockpit/bot.py 552  # 2026-09-25 +1: `typing.Any` import for registered_commands annotation. Prior 551: Task 2.2 (+5): register /cost command handler. Prior 546: 2026-09-25 Task 3.3 (+7): registered_commands method for /help command list
cockpit/gates.py 600
gtm_core/account_integrity.py 1135  # 2026-09-26 lowered to 1135. Prior 1138: 2026-09-26 lowered to 1138. Prior 1148: 2026-09-26 +6: classify evidence-too-short into GENERIC_LANE_STAYS_ERROR and forward lane/sources_dir to audit_records for W6. Prior 1142: 2026-09-25 +16 (PH10 +14, PH16 +2): `account_key` — per-account counts and dedupes key on `account_id` when the row carries one, the org token otherwise. The org token is the domain when present and the name when not, so an account with one domain-less row was counted twice and got every per-account finding twice. Public so `preflight_report` counts accounts on the same identity (it shrank by 7). PH16 +2: the new `signal-subject-short-form` WARN is classified row-level and stays-error in the generic lane, beside `signal-subject-absent-from-evidence`. Prior 1126: 2026-09-24 1107->1126: the generic lane's research-absence aggregates are `advisory` — printed, never budgeted (`AccountAudit.advisory`, folded into the acked set at `warn_verdict`, one summary line in the report). The operator was acking the same ten classes on every generic run by rote, and an ack nobody reads buys headroom for the classes they should have read. Prior 1107: 2026-09-23 +11: `demote_generic_advisory` extracted as a pure function so `preflight_report` applies the SAME generic-lane demotion the gate does (one implementation, no drift). Prior +63: P6 unlaned-gate fix, almost all of it comment. `--lane` becomes required=True (a live run gated a generic list with no lane, so personalised rules were applied to it and 138 contacts were reported blocked that were not); `why-now-not-a-signal` is classified into GENERIC_LANE_ADVISORY, and its consequence clause is made lane-aware because "this row would open its email with that sentence" is false where the field is never merged; and GENERIC_LANE_STAYS_ERROR is widened from the record rules to EVERY rule the gate can emit, so the two sets are exhaustive rather than "the record rules plus an unexamined default" — the gap the escapee sat in. No new branch: the widened set changes no behaviour (demotion keys on the advisory set alone), and the three rationales are written down here because the next person to make this flag optional will otherwise have to rediscover why it is not. Prior 1033 for 2026-09-21 -3: prospect-skill e2e fixes moved logic into sibling modules (ceiling lowered, never raised). Prior 1036 for 2026-09-10 +130: three enrollment-time refusal gates — `_refuse_parked_lanes` (a CSV row still carrying `lane` hold/excluded reaching `--require-verdict` enrollment: 52 excluded/already-enrolled + 14 hold rows measured at verdict=send on a live tenant pool with no --lane named), `_refuse_ambiguous_lane` (more than one enrollable lane in the CSV with no --lane to say which), and `_refuse_lane_state_mismatch` (the CSV's own `lane` column disagreeing with `lanes route`'s last recorded routing in evals/lanes-state.jsonl — a stale or hand-edited lane must not enrol on an old column's strength). Prior 1004 for 2026-09-09 +1: `LANE_VERDICTS` re-exported from the new gtm_core/lane_verdicts.py so gtm_core/prospects_consolidate can filter the hand-send list without an import cycle, and `--lane` choices now derive from LANE_VERDICTS keys instead of a hand-written list that had silently dropped `personalised`. Prior 1003 for 2026-09-03 +3: §R9 identity fictionalization (real academic-medical-centre and health-system examples reworded to generic phrasing and fictional domains) — the reword, not new logic, so the file stays the one filter implementation the dashboard and gate share
gtm_core/brandkit.py 501
gtm_core/build_eval_sheet.py 1263  # 2026-09-25 (PH13): threads `profile` through `load_live_rows`/`all_live_rows` so the eval-sheet builder's derived `seat` resolves against the tenant's own `role-vocabulary.toml` rather than the built-in default (the same `seat_of`/`persona_of` profile-threading fix applied to `gtm_core/cells.py`, `gtm_core/lanes/sheet.py`, and `gtm_core/email_campaign_dashboard/sources.py`, which had room to absorb it without a raise). The +1 is the one new `profile` parameter line — ruff's magic-trailing-comma rule refuses to fold it onto the prior line, and the call site and docstring were trimmed to bare minimum first. Prior 1262: 2026-09-24 (FR3 Finding A): a recipe's `rule` field is a CLAIM about the gate, and until now nothing checked it — eight recipes named rules the same day's retirement had deleted, so ~8 of every ~20 sheet rows planted a defect attributed to a rule `rule_lifecycle_report` has no denominator for, and every existing check passed them because the text HAD changed. The growth is `_Probe`, which runs the real gates (`lint_merge_render` + `signal_record.check_record`) over the mutant and the donor and requires the named rule to fire on one and not the other, plus `registry_recipes`, which builds the two derivation injections out of the tenant's own `do_not_say` phrase and `disputed` figure because neither may be a literal in `gtm_core` (§R4/§R9). Roughly half is comment: why the baseline is per recipe KIND (a pre-rendered mutant against a templated baseline reports every tag-dependent rule as newly raised), why `slot-attribution` gets a DECLARATION rather than a recipe (a slot line never renders, so a recipe for it would be the no-op this file already warns about), and what the text-kind baseline over-refuses. Eight stale recipes came OUT in the same change. Not taken, again: splitting the recipes into their own module — `_Probe` IS the verification the `_mutate_text` contract now promises, and separating them is how the promise goes stale. Prior 1031: 2026-09-24 +13 (FR3 Task 3.4): `SHEET_FIELDS` declares this module's subset of the one quality card (`gtm_core.messaging.card`) and is passed to `render_labeling_sheet`, so the sheet's questions are a DECLARATION a test can compare against the judge rubric's and the label schema's rather than a fourth list nobody diffed. Almost all of the growth is the comment recording why the subset is declared here and not derived. Prior 1018: 2026-09-23 +16: P1 threads `overlay` through `_load_matrix_if_present` -> `all_live_rows` -> `build_golden_set` -> the CLI, plus the `--overlay` flag's help text. An eval sheet built against an experimental hook grid must SAY so, or its conclusions are attributed to copy nobody sent — that sentence is most of the growth. Prior 1002: 2026-09-22 881->1002 (EC8): +4 copy injection recipes for the rules EC1 catalogued, the positional `_insert_sentence`/`_replace_offer` helpers they need, and the `_changed()` extraction that makes recipe viability independent of --n-injected. Most of the growth is the comment recording WHY these are text recipes (a row value cannot put a credit verdict into a template) and why they are positional rather than phrase-matching (phrase matching is how `hedge-missing` went stale) — the explanatory growth §R10 prefers a dated raise for. Not taken: splitting the recipes into their own module, which would separate a recipe from the `_mutate_text` contract and the no-op-verification comment that explain it.
gtm_core/capabilities.py 535  # 2026-09-26 lowered to 535. Prior 536: 2026-09-25 Task 2.1 (+7): technical connectivity probe and plain-English capabilities summary CLI. Prior 529
gtm_core/captions.py 1081  # 2026-09-11 +48: glyph colour derived from measured luminance (`_first_pole`) and `glyph_rgb` carried per screen into the sidecar — `palette.canvas` was assumed light, so a dark-theme kit drew every caption near-black over a dark picture and the contrast tier measured white instead of the type that was burned.
gtm_core/cells.py 839  # 2026-09-26 (+3): TOML formatting and parsing hardening. Prior 836: 2026-09-25 (+3): TOML formatting hardening and robust error handling. Prior 833: 2026-09-25 (+103): W9 attribution resolve_reply_cell and register_sequence support. Prior 730: 2026-09-21 -41: wilson/detectable_lift moved to the stdlib-only leaf gtm_core/power.py (re-exported here, so every caller is unchanged); prior 771: 2026-09-21 +30 for the OVERLAY dimension: cell_id becomes <overlay>:<segment>:<seat>:<variant> so two cells differing ONLY in targeting definition are a clean ICP comparison, `base` spelled out so historical ids keep their identity, and _mark_comparability gains the fourth dimension. Most of the growth is the docstring explaining why overlay is a DIMENSION and not a tag - trimming it would delete the reasoning the next reader needs. Prior 741: 2026-09-20 +1 for _safe_segment on CSV paths to eliminate path traversal. Prior 740: 2026-09-03 +139: lane on the cell map, lane/cell lookups by sequence, reply rate BY LANE with Wilson CI, and the cross-list overlap report — the oracle the lane design exists for
gtm_core/community_signal/render.py 663
gtm_core/content_quality/script.py 519  # 2026-09-11 Caption craft (K3): _caption_functions_check validates caption budget function column against sibling .shots.json.
gtm_core/deck_consistency.py 502
gtm_core/design_lint/rules_claims.py 551  # 2026-09-22 SD6-SD9, the claim rules: three of the four ship a shape that is NOT the one they were designed with, and the comment above each records the measurement that sent it back — a verb-list claim detector that was mostly wrong on real designs, a shared-term match satisfied by the product's own name, a prohibition detector aimed at the wrong register. These rules emit ADVISORY and so cannot fail CI: the reasoning is the only thing that stops the next reader re-deriving the discarded shape, and it is load-bearing
gtm_core/diagrams/drawio_extract.py 880  # 2026-09-21 +5: switch untrusted-XML parsing to defusedxml.ElementTree (XXE fix) and the comment justifying the still-safe stdlib Element import for in-memory construction/typing only. Prior 875: 2026-09-20 Diagram design Drawio IR extractor
gtm_core/diagrams/excalidraw_extract.py 690  # 2026-09-20 Diagram design Excalidraw IR extractor
gtm_core/diagrams/mermaid_extract.py 1324  # 2026-09-20 Diagram design Mermaid IR extractor
gtm_core/email_compliance.py 1066  # 2026-09-23 +23, all of it comment, for the UK+UAE re-admission: the `gb`/`england`/`scotland`/`wales`/`northern ireland`/`britain` aliases a row declaring "GB" needs so it is not silently dropped as out-of-market, plus the note that `normalize_market` strips a trailing period so the dotted `u.s.`/`u.k.`/`u.a.e.` keys could never match. No new branch or function — the executable surface is a dict literal. Prior 2026-09-21 +133 for the 2026-09-21 sequencer capability contract (SC9/SC2): --provider/--attest/--sequence-id flags and the record-autoset subcommand on the preflight CLI. The 400-line capability LADDER itself was NOT added here — it lives in gtm_core/capability_preflight.py, because judging what the provider does is a different question from judging what this sequence carries. Prior 2026-09-14 +51: reject region names (e.g. "Southeast Asia") in target_markets — a region never literally equals a lead's `country`, so left unguarded it either silently drops every real lead in it or, if ever auto-expanded, opens several jurisdictions at once with none of the per-country legal review the gate exists to force
gtm_core/eval_calibration.py 1845  # 2026-09-24 +22 (FR3 Task 3.4): `LABEL_FIELDS` now derives from `gtm_core.messaging.card`, the `claim_within_status` sub-check lands with the fact registry, and `render_labeling_sheet` takes the asking surface's declared `fields`. The growth is the field's docstring, its one line of operator guidance on the sheet, and the note on why `fields` is a parameter — not a second copy of the card. Prior 1823: 2026-09-22 1773->1823 (EC11): `rules --internal` joins the answer key, and the comment records WHY — the HTML labeler writes `injected: false` on every row by design, so the label's own flag is dead and P2's `keep` band was unreachable from a real round until this shipped.
gtm_core/gating.py 741  # 2026-09-25 Task 1.6 (-2): plain language gating copy. Prior 743: 2026-09-20 +20 for carve-excludes subcommand and exclude_mechanics policy support. Prior 723: 2026-09-16 +44: _public_doc_refs scans a stubbed skill's withheld files for docs/*.md citations that also ship publicly and threads them into render_stub_body, so a stub does not silently drop a citation a private-repo test (e.g. test_skills_reference_direct_response_patterns) expects to survive the carve. Prior 679 for 2026-09-11 +47: skill-manifest-rightsizing: extract/strip interface contract for stub_carve
gtm_core/gtm_distill.py 841  # 2026-09-21 +234 for IC4/IC5: the outreach distiller gains an axis classifier (_classify_axis + _STRUCTURAL_PREFIXES + _REMEDY), membership resolution from files already on disk (_membership), per-candidate MDE/powered, a separate Watch section and a non-list-tags refusal. Most of the growth is docstring: why a seatless persona is NOT a finding, why structural and unknown name no file, why DEFAULT_MIN_SENT stays at 5 against a ~3,070/arm detectable effect, and why an underperformer's effect size is a magnitude rather than a raw ratio. Trimming that deletes the reasoning that stops the next reader reinstating the defect
gtm_core/hook_coverage/matrix.py 538  # 2026-09-24 first listing, 500->538 for FR2's ROW AXIS. `hook-matrix.md` became a generated view of angles.toml whose rows are SEATS, not personas, so this module needed a second key space and a dispatch between them: RowAxis, seat_key_of_label, row_key_of_label/row_key_of_title, Matrix.row_key/recipient_key, and a _detect that reads shape AND axis from the one header cell (a file naming both axes is refused, not guessed). ~120 of the ~157 added lines are the WHY, and it is the load-bearing part: `ceo` and `cto` are legal keys in BOTH spaces, so a report that resolved row labels as seats and recipients as personas would look right and count wrong with no defect anywhere to find it — that reasoning is why row_key and recipient_key are a PAIR on the matrix rather than two free functions a caller can mix. The §R10-preferred remedy, splitting the axis vocabulary into gtm_core/hook_coverage/row_axis.py, is the right next move but not in this commit: the resolvers need `_clean_cell`, which lives here and is imported from here by rows.py, messaging/matrix_view.py and the package __init__, so the split either moves a parser-owned normaliser out of the parser or introduces a cycle — and three other FR2 tasks are editing this package concurrently.
gtm_core/hook_score.py 708
gtm_core/hooks.py 912  # 2026-09-24 +26 for the silent-empty fix in `_parse_hook_matrix`: FR2 made `hook-matrix.md` a GENERATED seat x premise grid with no `id` column, so the fallback parser's `table_re` matched nothing and returned a bank of ZERO hooks with no defect - a gate that passes by finding nothing. Four of the added lines are the refusal; the rest is the WHY, and it is the load-bearing part: it records that the two files answer different questions (a rendered argument grid is not a hook bank, so one is not a fallback for the other), that the detection reuses `matrix_view.is_generated` so there is one definition of "generated" rather than a second banner test drifting here, and that the import is function-local because a hook-bank load must not pull the messaging registry in at module import. Latent at the time of the fix - every live profile has a `hooks.toml` and never reaches the fallback - which is exactly why it needed a comment and not just a raise. Prior 886: 2026-09-23 +23: P1 threads `overlay` through `hooks_toml_path`, `hooks_matrix_path` and `load_hooks` so a messaging experiment can vary the hook bank and not only the audience. The comment on `hooks_toml_path` records why the rung is an argument and never an env var (an ambient experiment outlives the run that asked for it), and `promote_hooks_toml` is deliberately NOT given one — it is a writer into `profiles/`, and a promote that could target an experiment directory would make an experiment authoritative.
gtm_core/knowledge_meta.py 607  # 2026-09-24 +27 more, hardening that exemption into a CLOSED ALLOWLIST after review: the first version matched any `gtm_core.<anything>:generated` comment on line 1, and its stated defence — that a banner buried further down cannot claim the exemption — guarded the wrong attack. The threat is one paste at the TOP of `product.md` or `case-studies.md`, which would have removed either from this gate entirely, and unlike `hook-matrix.md` NOTHING regenerates those, so there is no drift check behind the exemption: the file just stops being checked for staleness by anything. It is now a `(filename, banner)` allowlist where both halves are required — the filename alone would exempt a tenant's hand-kept matrix of the same name, the banner alone lets any topic opt itself out — and adding a pair is an explicit claim that a named generator regenerates that exact file. The growth is that reasoning plus `_generated_banner`. Prior 580: 2026-09-24 +28 for the GENERATED-view exemption: `hook-matrix.md` became a file rendered from `angles.toml` (FR2), and a managed topic that is generated has no lifecycle metadata of its own to seed. The rule itself is four lines; the growth is the `generated` field on KnowledgeMeta plus the comment block recording (a) why the exemption is content-based — keyed on the generator's banner — and not by filename, since other profiles still hand-keep a `hook-matrix.md` where the frontmatter is real and a name exclusion would switch the gate off for all of them at once; and (b) why `refreshed:` must not simply be seeded here — it would be a second home for the SOURCE file's freshness, it would move on a regenerate that changed nothing, and it would break the generator's own drift check, which compares committed bytes against a fresh render and so has to stay date-independent. Not a weaker gate: frontmatter answers "is this stale?" by a date someone remembered to bump, the drift check answers it exactly. Prior 552
gtm_core/material_intake.py 543  # 2026-09-25 +13: stage-verb hardening after review; prior 530: Task 2.3 (R-9) material intake pdf/docx docling conversion and stage CLI verb
gtm_core/metering.py 860  # 2026-09-20 +289 for unified credit wallet and atomic reservations support. Prior 571: 2026-09-16 +40: workspace_jsonl_month_total() plus the comment explaining it — the backend cap read only Postgres cost_records while every agent/mcp/* worker meters to the workspace costs.jsonl (stdio subprocess, no pool, no workspace id), so worker spend was invisible to a multi-tenant billing boundary. The two stores are disjoint, so the cap sums both; the prose is load-bearing because the disjointness is the assumption that makes summing correct rather than double-counting
gtm_core/optout_watch.py 641  # 2026-09-24 +62 for is_clear_optout/typed_text: the operator decided a clear opt-out is added to DNC by the sweep without a gate, so the recall-biased matcher needed a precision-biased sibling that reads only what the sender TYPED — live 2026-09-24 a Gmail reply is one line of HTML, the line-based trim never cut it, and the reply matched on the words of our own quoted email. The quote-start table and the note on why the two tests are biased in opposite directions are the growth. Prior 579: 2026-09-22 raised 541->582 for _reply_head: a live read of setting code 2 showed all seven sequences instruct the recipient to reply with the single word 'stop', and the bare-stop branch missed six of eight realistic shapes of that reply because a phone signature or a quoted original pushes the body past the word cap. The marker table and the note saying the trim may only ever ADD a match are the growth. Earlier, same date, first entry: +41 over the 500 default for the 2026-09-21 sequencer capability contract: SC6's non-Latin-script detector, SC12's soft-no split, and the LIVE-VERIFIED field readers (message_body/message_sender/message_ts/message_is_inbound/thread_id_of/thread_ts). The last group is the reason this file had to grow: every field name here was an unread guess and all of them were wrong, so the sweep read nothing from a real account. Each reader takes the live name first and the assumed one second, and says which is which — the comment IS the growth.
gtm_core/outcomes.py 686  # 2026-09-16 +8: narrow unattributed()'s finish-*.json glob to finish-<digits>x<digits>.json so video_finish.execute's new finish-spec-<ratio>.json sidecar is never fed to load_finish (it isn't a FinishManifest, and raised an uncaught TypeError). Earlier reasons, 2026-09-11 +28: K8 _caption_attribution() reads score.json's TOP-LEVEL caption_mode/caption_voice/describe_share so the attribution row carries them as queryable tags. 2026-09-07 +25: _story_format() reads score.json's TOP-LEVEL story_format; 2026-09-03 +3: OPTOUT_OUTCOMES so the ledger knows who said stop
gtm_core/preflight_report.py 804  # 2026-09-26 (+8): import account_key from router and forward lane/sources_dir/profile to audit_records for W6. Prior 796: 2026-09-25 Task 3.2 (+28): preflight_refusal and plain summary line above engineering block. Prior 769: 2026-09-25 -7 (PH10): the whole-file account count reuses `account_integrity.account_key` instead of its own org_token comprehension. Prior 776: 2026-09-24 749->776 (PS15): the report now carries `readiness` — every send-list row's fate under the enrollment gate's own rules, with the refusal classes of any batch the gate would refuse — because this report is the one place the checks' answer is persisted and the status block and dashboard now READ it instead of re-running the audit and keeping only a count. The computation lives in prospect_readiness.py; the growth here is the field, its docstring, the `to_dict` key, and a five-line `_readiness` that hands the lane partition in (so `_lane_groups` stays the one partitioner). Prior 749: 2026-09-24 +34 (PH9 + invariant review): `_across_groups` stops the NUMERATOR repeating the mistake the account count made — `audit_rows` dedupes its account tier per CALL, so an account whose rows sit in two lanes reported `no-dossier` once per lane against a denominator that is now distinct over the file. Also records why the lane column is taken on trust in a read-only report when the enrollment gate refuses to. Both are mostly comment: the next reader needs to know that the two halves of one rate are computed over different populations unless something keeps them together. Prior 715, +13 (PH9): the account count is now distinct over the whole file instead of summed over the lane groups. A live 650-row run reported 603 accounts against 578 real ones, because 25 accounts have rows in two lanes at once — and that total is the `denominators` base every WARN-tier rate divides by, so the inflation understated all of them. Almost all of the growth is the comment recording that, because the next person to see `accounts` computed outside the loop will otherwise put it back. Prior 702: 2026-09-23 +47: the daily unit audited a MIXED-lane file unlaned, so every routed generic row failed it on findings the gate waves through (the P6 defect one door over). `_lane_groups` partitions by the row's `lane` column and both record-tier checks run per lane; about half the growth is the WHY.
gtm_core/prospects_import.py 634  # 2026-09-22 +60: `finalize` now REFUSES a scored item that does not name the rubric it was scored against (§R19 / PRD 2026-09-22) — the growth is the refusal, its kill switch, and the docstring explaining why a score is a claim that has to cite something: a 1003-row run merged numbers from an undeclared rubric that looked identical to numbers from the real one. Prior 574 for 2026-09-21 +21: --source couples provenance to billing so a curated list-mode sheet cannot log fabricated §R2 spend (~$16 on 400 rows); the growth is the docstring stating that coupling + the flag. Prior 553 for 2026-09-21 -78: prospect-skill e2e fixes moved logic into sibling modules (ceiling lowered, never raised). Prior 631 for 2026-09-20 +131 for standard-mode item inflation and stage-standard CLI. Prior 500
gtm_core/prospects_state.py 737  # 2026-09-26 lowered to 737. Prior 739: 2026-09-26 (+5): protected fields and serialization hardening. Prior 734: 2026-09-25 (+16): _PROTECTED_ACCOUNT_FIELDS protection in fill_accounts. Prior 718: 2026-09-25 (+90): W4 firmographics fill_accounts batch writer under serialised lock. Prior 628: 2026-09-25 +4: `mutate` stamps `verdict_on` when it sets `verdict`, the research date `prospects_consolidate` compares before lifting a row's re-angle to send. Prior 624: 2026-09-22 +5: _identity_keys docstring records the MC-01 guard — an exact-name collision no longer merges rows whose domains differ. Prior 619 for 2026-09-21 -1: prospect-skill e2e fixes moved logic into sibling modules (ceiling lowered, never raised). Prior 620 for 2026-09-20 +5: preserve added_at timestamp on upsert_latest. Prior 615
gtm_core/render_manifest.py 700  # 2026-09-11 +7: `transitions` on FinishManifest — the non-cut joins the stitch applied, carried off the master's sidecar so a shot list's declared production.transition_in has evidence to be checked against at finish; nothing else in a finished file can say whether a declared dissolve happened. Prior 693 for 2026-09-11 +23: `captions_preburned` + `overlays` on FinishManifest and a kit-declared `captions_route` waiver — a per-shot burn stitched into a master finished with `captions: null`, which silently disarmed V3/V11 on the one film that needed them.
gtm_core/screen_ui/cli.py 521  # 2026-09-16 first listing: `glittery-sleeping-yao` changes #3/#4 — `render_scene()` extracted so the rebuild driver can call it per shot without re-invoking `main([...])` (which re-reads the kit file and prints on every call), the build-record refusal/warn/delete-before-render wiring around it, and the new `fit-layout` pseudo-scene's three flags (`--for-scene`/`--caption-lines`/`--margin-px`) dispatching to `screen_ui/fit_layout.py`.
gtm_core/screen_ui/scenes/chat_bubble.py 515  # 2026-09-11 first listing: the one scene that composites a message bubble beside a phone on a provider render; carries its own fit-or-refuse (names the smallest w_frac that fits) and glyph refusal so an emoji cannot ship as a tofu box.
gtm_core/signal_record.py 816  # 2026-09-26 (+6): explicit Finding calls for category_relation ast rule extractor. Prior 810: 2026-09-26 (+43): source capture validation and audit records. Prior 767: 2026-09-25 (+7): R0.2 structural signal freshness check. Prior 760: 2026-09-25 lowered to 760 (W6 captured source verification). Prior 765: 2026-09-25 +120 (PH16): the subject checks recognise the SAME company written the short way without passing a namesake. `_name_forms` gives the full name and its core (leading "The" and a small explicit set of trailing descriptors dropped); `_named_in` accepts a form word-bounded and capitalised in the evidence unless the word written straight after it is a qualifier the subject lacks ("Acme Insurance" for "Acme Health", "Acme plc" after a shortened core; a legal form after the FULL name is its own) or a domain suffix ("Acme.ai"), and a one-word SHORTENED core only as a possessive or before a capitalised product name (a one-word FULL name is the row's own and is exempt — accepted residual: a dictionary-word name can pass on a sentence-initial use); a subject that is the company's core is a new WARN `signal-subject-short-form`, never silent and no longer an ERROR. Replaces the flattened-substring test, which itself passed "Acme Health" inside "Acme Healthcare". Roughly half is the comment on why each guard exists. Kept beside `normalise_company`, not in `clean_company`, which renders names. Prior 645: 2026-09-11 +5: signal record sync. Prior 645 for 2026-09-10 +6: `check_record` falls back to `signal_clause(why_now)` when the row carries no stored `signal_clause` — that column is derived, never one of ready-to-load.csv's 41 stored columns, so it was blank on every pooled row and the provenance check silently took the "nothing to source" exit on rows that in fact made a dated claim. Prior 639 for 2026-09-03 +4: JUDGE_COLUMNS gains judge_defect_class (the normalised routing key) plus the comment on why judge_verdict_reason cannot serve as one
gtm_core/spoiler_check.py 541
gtm_core/suppression.py 742  # 2026-09-24 676->742 for the `write-dnc-cache` verb: the consolidate-time DNC cache had a documented shape, a reader that refuses it stale, a reconcile verb that read the same provider payload — and no writer but a hand, so it went stale between them. The growth is the verb, its refusal of an empty read, and its parser entry; most of it is the docstring recording that gap. Prior 676: 2026-09-21 +9 for the 2026-09-21 sequencer capability contract (SC8): `_norm_domain` gained a docstring and a `www.` strip, and `reconcile_dnc` stopped re-implementing that normalisation inline. A DNC entry for `www.bracken.example` did not cover `dana@bracken.example`, so a domain-wide suppression half-applied — matching narrower than the identity is the dangerous direction here. The growth is the comment explaining why, not new branching. Prior 667: 2026-09-21 first listing, +175: `reconcile_dnc` plus its payload normaliser (`normalize_dnc_payload` and the three shape helpers it splits into to stay under C901) and a `reconcile-dnc` CLI verb. The module docstring had promised since it was written that a `dnc-optout` row "MUST also be on the provider DNC list", and then said verify() could not check it because "the connector has no read-back" and the operator would confirm once. Both halves were wrong: the read-back existed (list_dnc_lists -> get_dnc_items_by_id, which prospects_consolidate/paths.py had been documenting as the DNC cache's own source all along), and the once-confirmation never happened — three real opt-outs sat on the provider list from 2026-08-11 to 2026-09-21 with no row here at all, so the wave gate could name none of them. Most of the added length is the two docstrings, and they are load-bearing: one records why the reverse direction (a provider entry with no ledger row) is deliberately NOT a finding — the provider list legitimately holds ~50 hand-added exclusions that the freshness-gated consolidate cache already blocks on, and reporting them would bury the one direction that matters under entries working as intended.
gtm_core/vo_timings.py 524
gtm_core/wave_gate.py 532  # 2026-09-21 first listing, +96: `WaveReport.opt_out_emails`, `unsuppressed_optouts`, and the three ingest-time refusals that use them. A wave record counted opt-outs and named nobody; the provenance of the count lived in the free-text `source` string, which nothing can parse, so `optout_reconciliation` could not tell an accounted wave from an unaccounted one. The 2026-07-29 wave recorded `opt_outs: 3` with the three people written nowhere the send path reads, and the refusal that should have caught it fired only at `check` time six weeks later — where it was also overridable. Ingest is when the provider payload is in hand, so ingest is where naming is demanded: refuse a count with no names, refuse a name not suppressed anywhere the send path reads, and refuse a partial list (which would otherwise reconcile as if it were complete). --ack-unnamed-optouts is the recorded override, matching --ack-high-optout.
mcp_server/server.py 620  # 2026-09-20 +31 for atomic credit reservation and settlement. Prior 589: 2026-09-20 +6 for 402 InsufficientCreditsError trapping in draft_post and draft_outreach. Prior 583: 2026-09-19 +21 for gateway secret enforcement and prompt size caps
plugin/skills/account-dossier/scripts/render_dossier_pydocx.py 729
plugin/skills/account-plan/scripts/render_account_plan_pydocx.py 642
tests/linter/content_linter.py 901  # 2026-09-20 +9: staccato rewrite from regex to segment-splitting with word-count threshold to eliminate false positives on normal prose
tests/linter/outreach/cli.py 625  # 2026-09-24 +10 (FR3 invariant review, finding 3): `pack` mode gains `--profile`, and `_run_pack` loads the registry ONCE into the shared kwargs so a `--batch` run cannot silently lint under a narrower gate than the same packs linted one at a time. Before this the subcommand had no such flag at all, so the 1:1 path — the shape a human reviews and sends — reached none of the registry rules while `gtm_core/messaging/card.py` recorded `claim-status` as authoritative on it. Prior 615: 2026-09-24 +49 (FR3 Task 3.5): `--selftest` now loads the tenant ban files (`_tenant_ban_phrases`) and both positive controls were rewritten to the registry-derived five-slot shape — net of +112 fixture/comment lines against -70 from folding the two render controls into one three-touch spec. The growth is the two FR0-residue arguments the next reader needs: why the selftest unions every profile's ban file rather than naming one (a tenant path in `tests/` is a de-brand failure, and house copy should be sendable for every tenant beside it), and why a control names an anchor by ROLE rather than by name (the 2026-09-22 §R9 breach, where an "invented" acronym was a tracked competitor's donated spec). Prior 566: 2026-09-24 +28 (FR3 Task 3.2/3.3): `--capability-file` and `--anchor-report` deleted with the rules that read them, and two helpers added — `_load_registry`, which turns the three derivation rules on whenever `--profile` is given rather than behind a flag of their own (an opt-in check nobody opts into is how `cta-unstaged-artifact` sat inert for months), and `_notice_retired_flags`, which keeps `--artifact-file`/`--hook-matrix` accepted but prints that nothing reads them, so a skill command string mid-migration neither dies at argparse nor silently reads as a check that ran. Most of the growth is that reasoning; both helpers go when the skills stop passing the flags. Prior 538: 2026-09-24 first listing (FR3 Task 3.1).
tests/linter/outreach/rules_batch.py 582  # 2026-09-24 +54 (FR3 invariant review, finding 3): both pack entry points now call `lint_derivation` through `_derivation_on_a_pack`. Most of the growth is the §R5 argument the next reader needs and cannot re-derive: a 1:1 pack body is already RENDERED, so `_copy_under_test`'s template-only guarantee does not hold there and what replaces it is one-directional — row-derived text may only ADD a refusal, never suppress one — which is a property of how every rule is written and would be broken by the first rule that CLEARS a finding on evidence found in the copy. Also why `rows=[]` (a pack has no country column, so the foreign-anchor branch is silent by absence rather than by exemption) and why the scope label is PACK rather than SPEC. Prior 528: 2026-09-24 -46 (FR3 Task 3.2): `lint_hedge_stem` retired with `hedge-stem-repeat`, and the pack entry points lost their `gift_artifacts` / `capability_rules` pass-through. Prior 574: 2026-09-24 first listing (FR3 Task 3.1). Rules that need more than one email (template share, thread repetition, touch counts) or judge the pack document (rules-version staleness, format detection, the tracker CSV), plus the pack entry points.
tests/linter/outreach/rules_derivation.py 563  # 2026-09-24 first listing, 481 -> 563 (+82) for the §R18 fix to `proof-status`'s figure predicate. It shipped that morning as `\d[\d,.]*` — any digit run — and the growth is the predicate that replaces it (a number counts only with a unit or a comparative: `_NUM`, `_UNIT_ALT`, the three-alternative `_FIGURE_RE`, and the unit-folding tables `_figures` normalises through) plus, mostly, the reasoning a future reader cannot re-derive from the regex: the two live false positives it fixes ("a 90-second demo recording" convicted as the retracted 90% proof; a prospect's "35 million dollar" round convicted as a disputed incident rate), the saturation measurement that sent it back (49/66 warnings, 74.2%, exemplars `1`/`1.5`/`110`), why ADJACENCY is the line (a hyphen does not join a number to its unit, so an attributive compound is not a quantity) and why a bare scale word and a plural noun are deliberately not units. The severity comment was also re-measured both ways over one corpus rather than left arguing from a rate the fix invalidated. Not split: `_figures` and `lint_proof_status` are one rule — the normalisation IS the comparison — and separating them is how the two sides of a figure comparison drift apart, which is the bug being fixed.
tests/linter/outreach/rules_render.py 635  # 2026-09-24 +2 on top of the 633 below: a second §R9 rewrite, from the pre-commit carve-preflight pass. `lint_signal_contradiction`'s docstring named three real operating companies as accounts from a live batch, each with its researched clause attributed to it — the same shape as the 633 entry, one function down, and the same reason it is invisible to every automated gate: a bare company name has no shape for `pii_check` to match and these were not in the roster's derivation. The counter-evidence clauses stay (they are the lesson); the roster is theirs, not ours. Prior 633: +6 on top of the retirement's -319: the §R9 rewrite of `lint_signal_pitch_collision`'s widening note. It attributed each competitor vocabulary to the vendor that used it, and those names are `aliases` in the tenant's competitors.toml — which `tests/lint/third_party_roster.py` never reads (it derives from account folders and the case-study list), so a competitor's name in a comment HERE is invisible to every automated gate while `tests/` ships wholesale in the carve. The replacement states the shape only and records why the alternation below it keeps its literals: those are the matcher, and fictionalizing them would break the rule. Prior 627: -319 (FR3 Task 3.2): the four DECLARATION rules deleted — `lint_hook_cell`, `lint_signal_cell`, `lint_signal_column`, `lint_stakes` — plus `_company_invisible`, which existed only for `specificity`'s exemption. Each of the four checked that one hand-written front-block field agreed with another; the registry makes all four DERIVATIONS of one declared `angle:`, so the loader answers the question and no rule needs to. Prior 946: 2026-09-24 first listing (FR3 Task 3.1).
tests/linter/partnership_brief_linter.py 540
gtm_core/funnel.py 565  # 2026-09-24 +32 (PH8: the file had THREE readers of funnel-yields.toml with three different rules for what a yield is, and they disagreed in both directions — load_yields range-checked but took a TOML `true` as a 1.0 yield (a gate that refuses nobody), _read_yields_file excluded bools but took any magnitude so an out-of-range value was written back and then silently dropped on the next read, and record_actuals validated stage names against a closed set while validating no value at all. Collapsed to one predicate, `valid_yield`. Nearly all the growth is its docstring: the three-way divergence is the thing a future reader must not reintroduce, and a one-line predicate with no WHY is exactly how it came back the first time). Prior: 2026-09-23 +217 (+11 for P5b: real data showed the stage table collapsing five MEASURED gates to a bare dash because one unmeasured gate sat in front of them, so the renderer now has three bands — counted, rate-known-count-unknown, unmeasured — and states both facts instead of quietly choosing one). Prior +206 over the 500 unlisted cap: P5a extends STAGES from the five DISCOVERY stages to the eleven a real run meets, adding the six REFUSAL gates (compliant, unsuppressed, list_fit, integrity, lane_enrollable, cell_assignable) that decide whether a row may be enrolled. Most of the growth is the WHY: the six ship with NO DEFAULT_YIELDS entry, and a future reader's obvious tidy-up — adding 1.00 so the renderer stops saying `unmeasured` — silently forecasts zero loss at six gates that refuse real rows, which is the surprise the module exists to remove. That reasoning is a comment block, an `unmeasured` path through size()/check_stage()/to_dict(), and `render_stage_table`, which orders by leak size because the operator's question is "where did my 500 go?" and pipeline order buries the answer. Splitting the renderer out would put the table and the stage set in two files that must agree, which is the failure mode this module already documents.
gtm_core/lanes/router.py 573  # 2026-09-26 lowered to 573. Prior 574: 2026-09-26 (+16): automated formatter applied and wedge seat champion check. Prior 558: 2026-09-25 (+4): R7.6 send decision handling in _apply_decision and route_row. Prior 554: 2026-09-25 W7 champion-missing hold and W1a judge advisory routing
gtm_core/role_vocabulary/__init__.py 608  # 2026-09-26 lowered to 608. Prior 609: 2026-09-25 lowered to 609. Prior 617: 2026-09-25 (+71): W7 level_mix, level_mix_report, and wedge_seats support. Prior 546: 2026-09-24 first listing, 500->546 for the outbound fact registry: `[[seat]]` gains lead_pain / gain / forbidden_pains / register / segments, the five accessors that mirror `stakes_for`, and three refusals (a non-string lead_pain, a register outside the closed set, a seat scoped to a segment the file never declared or to the `unspecified` bucket). Most of the growth is the WHY, in this module's own style: each refusal carries the failure it prevents, because the alternative to refusing is always silent — a scalar `forbidden_pains` forbids every CHARACTER of itself, an unknown register falls back to one the tenant never chose, and a seat aimed at an undeclared segment reads as covered while none of its copy can ever be selected. A further split was considered and rejected: `defaults.py` already carries the DATA, and moving the copy-field validation out would put it in a different file from the dataclass it fills, which is the two-files-that-must-agree failure the module was split to avoid, not repeat.
gtm_core/role_vocabulary/defaults.py 507  # 2026-09-25 (-15): restored built-in DEFAULT_SEAT_RULES to shipped defaults (security covering ciso, compliance, data-compliance). Prior 522: 2026-09-25 W7 default personas for partnerships, risk-compliance, and innovation seats
gtm_core/send_cards.py 820  # 2026-09-26 lowered to 820. Prior 825: 2026-09-26 (+50): automated formatter applied. Prior 775: 2026-09-26 (+2): exempt backend workspaces from send-cards requirement per PRD Section 3.C. Prior 773: 2026-09-25 (+63): HTML escaping, unique draft filenames, and level_mix integration. Prior 710: 2026-09-25 W8 send cards generator, decisions, and apply CLI
gtm_core/messaging/cli.py 621  # 2026-09-24 620->621: the resolve text line names how the premise was attested (`record` / `industry` / `seat`) beside the angle id — an operator reading a generic pool resolving mostly on the seat needs to see that, or a list nobody researched reads like a list fully argued. Prior 620: 2026-09-24 616->620: `resolve` reports the row's `segment` beside `market` in both the JSON row and the text line, defanged at the same one place for the same reason (`clean_segment` passes an unrecognised value through as written). An operator acting on the new `segment-unresolved` refusal needs to see what was read, or the refusal is a silence with extra steps. Prior 616: 2026-09-24 first listing, 500->569 for FR2's TWO write verbs, landed in this one file by two tasks. (a) `matrix` (Task 2.4) regenerates hook-matrix.md from angles.toml and refuses a target without the generated banner; `check` gained the drift half, which carries the reason only a STALE matrix is a defect while ABSENT and HAND_AUTHORED are not — most profiles here keep a hand-kept matrix, and failing them would make the gate red for everyone who has not migrated, which is how a gate gets switched off. (b) the fifth key-scoped `profiles/` writer: the `angle promote|retire` sub-verb, its own option group, and the dispatch. The option group is written out rather than composed from `_add_common` on purpose — it is `_add_common` MINUS `--overlay`, and "same as the read verbs except one" is exactly the shape that silently regrows the excluded flag, which here would let a promotion decided under a one-run experiment overlay land in the tenant's live angles.toml. The rest of the growth is the WHY: which verb writes, why promotion needs a cited outcome cell while retirement does not, and why the evidence argument is parsed as numbers rather than accepted as the operator's string (its figures derive from reply text, §R5). A split of this file into read verbs / write verbs is the §R10-preferred remedy and is the right next move, but not in this commit — two other FR2 tasks are editing the same file concurrently, and a reorganisation under that is how one of the three loses its verb. (c) 569->582 same day, for the §R5 defang on the REFUSAL path: `_fail` now renders every operator-fixable line through `_inert`, and the docstring states why it is done there rather than at each of the six refusal sites — `registry._shaped` guards an id with `_safe_segment`, which rejects `/ \ .. NUL` and NOT `⟦`/`⟧`, so a loadable id can carry a gate marker into a run header. A seventh refusal message is now defanged by construction rather than by the author remembering. (d) 582->616 same day: (c) was half the fix. `_fail` is not the one place a refusal is rendered - `unused` printed its own defect loop straight to stderr and interpolated an angle id, seat, premise and status into a bare `print` on the SUCCESS path, both undefanged, and a success line is the one a brain is MORE likely to paste into a run header. The growth is `_say` (every line this module prints goes through it, and a test fails the file on a bare `print`, so the next printer is defanged by construction), `_inert_all`, and a docstring stating the rule that kept being got wrong: TEXT is defanged at the print, JSON is defanged as DATA when the payload is built - `json.dumps` escaping U+27E6/U+27E7 is a property of `json.dumps`, not a control, because the consumer's `json.loads` hands the live marker back.
gtm_core/messaging/registry.py 552  # 2026-09-24 508->552 for the SEVENTH cross-block rule: a claim marked `boundary = true` may not ride a premise that declares `attests_boundary = false`. The rule is six lines; the growth is `Claim.boundary` with its docstring, the bool parse mirroring `Proof.binding`, `_load_premises` returning the whole entry rather than the key set, a type-only import block, and the docstring paragraph recording WHY the rule exists — the 2026-08-25 refutation (47 of 56 rows judged `drop` for assuming a boundary the evidence never showed) lived only as a comment in `premise-vocab.toml`, so the 09-24 angle mining put eleven angles on that premise and nothing could object (§R13). Conditional on both sides having SAID, because every vocabulary written before the field existed is unlabelled; `retired` exempt, because history is not an offer. Prior 508: 2026-09-24 first listing, 500->508 for the SIXTH cross-block rule in `_check_references`: a `live` angle may only cite a `verified` claim. Both the PRD and the FR1-FR3 plan described that state as unrepresentable and it was not — it was refused only by the writer (`angle_status.promote`), which is worth nothing against a tenant file a human edits in an editor, and the gap was found as a strict xfail in the tenant-data angle contract test rather than by either doc. The rule itself is four lines; the growth is the docstring paragraph recording why it is conditional on `status` (an angle on a conditional or design-target claim is legitimate while it is `draft` — refusing the claim alone would delete the tenant's entire backlog of arguments waiting on a verification) and why the claim is fetched with `.get` (a dangling claim is already reported above, and a second heading sends the operator looking for an edit that does not exist). A split was considered and rejected: the closed sets, the key allowlists and the cross-file checks have to agree, and putting them in two files is the two-files-that-must-agree failure the module documents everywhere else.
gtm_core/lanes/decisions.py 536  # 2026-09-26 lowered to 536. Prior 538: 2026-09-25 (+1): R7.6 send decision support. Prior 537: 2026-09-25 516->537: hold-apply no longer skips a same-decision entry whose `detail` differs from the ledger's as "already recorded" — the router honours a decision only on an exact detail match, so a decision recorded against the composed `lane_reason` could never be corrected (the 2026-09-25 restage recorded a throwaway `salvage` first to force it). The growth is the `detail_updated` bucket, the hold CSV's raw `detail` column, and the docstring recording that the sheet export used to fill `detail` with the composed string (tests/unit/test_hold_apply_detail_update.py). Prior 516: 2026-09-24 513->516: the state record carries the judge's defect class, note and calibration flag beside the verdict, so `consolidate._stamp_lanes` can own every judge column from the last route instead of the pool carrying August verdicts forward. Prior 513: 2026-09-24 first listing, 500->513 for the hold-apply suppress scope: a filled hold sheet's `suppress` retires the COMPANY only for question types whose sheet copy said so, and retires the one contact otherwise — the growth is the per-type scope table and the reason it is data rather than a blanket rule (tests/unit/test_hold_suppress_scope.py).
gtm_core/prospects_consolidate/consolidate.py 549  # 2026-09-25 536->549 (Gap 2): a row's `re-angle` lifts to the account's `send` when the account's research stamp `verdict_on` is newer than the row's and the account carries a complete signal group (`accounts._verdict_promotes`); verdict, reason and stamp move as one unit. Operator-approved exception to promote-never. 521->536 (Gap 3): `signal_state: cleared` on an account blanks the whole atomic signal group on its rows. A blank record means "no opinion" and never overwrites, so re-research that found NO signal could not remove the old clause's provenance, and account_integrity blocked those rows. The growth is the blanking loop, its comment, and two result counters. Prior 521: 2026-09-24 first listing, +21 over the 500 default: `_stamp_lanes` now owns the judge's four columns from the lanes-state record (blank when the route attached none) instead of the rolling master carrying a verdict forward with no record of which spec it judged — on 2026-09-24 the pool held 283 `re-angle` / 220 `drop` from August specs while the state file held none. The growth is `_JUDGE_STAMPS`, the stamping loop and the comment recording why a blank is the honest value.
