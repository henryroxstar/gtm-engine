# tests/lint/complexity_allowlist.txt — the §R10 file-length ratchet (docs/RULES.md).
#
# One entry per production file over 500 lines:  <repo-relative path> <ceiling>
# A listed file may shrink but never grow past its ceiling. When it shrinks, lower the
# ceiling in the same PR; when it drops to 500 or fewer lines, DELETE the entry (a stale
# entry fails the check). Raising a ceiling, or adding an entry, requires a dated reason
# on the line:  path 620  # 2026-09-02 <why this file must grow>  — the check compares
# against the committed list, so growth is a recorded decision, never a silent bump.
# Each raise needs its OWN reason: one written for an earlier raise does not cover a new one.
#
# Regenerate from scratch (drops every note — a re-freeze, not a routine edit):
#   uv run python tests/lint/complexity_check.py --freeze > tests/lint/complexity_allowlist.txt

agent/mcp/apollo/server.py 979
agent/mcp/rocketreach/server.py 578
agent/mcp/saleshandy/server.py 754  # 2026-09-03 +10 for the NOT_CONFIGURED sentinel and the comment explaining why 'connector absent' must stay distinguishable from 'API failed' — agent/optout_sweep.py compares against it by identity to stay quiet on the former while still exiting nonzero on the latter
agent/permissions.py 982
agent/publish.py 704
agent/session.py 546
cockpit/bot.py 539
cockpit/gates.py 600
gtm_core/account_integrity.py 1003  # 2026-09-03 +3: §R9 identity fictionalization (real academic-medical-centre and health-system examples reworded to generic phrasing and fictional domains) — the reword, not new logic, so the file stays the one filter implementation the dashboard and gate share
gtm_core/brandkit.py 501
gtm_core/build_eval_sheet.py 881
gtm_core/campaigns_dashboard.py 646  # 2026-09-04 +4: carry `roster_globs` through the campaign shaper. The shaper reshapes each manifest into a fixed key set, so a campaign declaring where its own accounts live had that declaration silently dropped — and a campaign-scoped page could then only count the shared prospect pool, rendering "731 people we will actually email of 859 loaded" for a run whose whole roster is 26 accounts
gtm_core/captions.py 1033  # 2026-09-05 +67 for `_wrap_balanced` (balanced caption line wrapping), added in the 2026-09-04 evening session. REASON RECORDED, NOT AUTHORED: this entry unblocks the suite for everyone; the author of that change should replace this line with why the file must carry it, per §R10. Prior ceiling 966 for 2026-09-03 +129: measured-backdrop glyph colour (backdrop_luma, caption_box_crop, _resolve_glyph_rgba) — the caption box geometry and the luma it is sampled against are the same fact, so splitting them puts the crop in one file and the threshold it feeds in another
gtm_core/cells.py 740  # 2026-09-03 +139: lane on the cell map, lane/cell lookups by sequence, reply rate BY LANE with Wilson CI, and the cross-list overlap report — the oracle the lane design exists for
gtm_core/community_signal/render.py 663
gtm_core/deck_consistency.py 502
gtm_core/email_compliance.py 858
gtm_core/eval_calibration.py 1773  # 2026-09-03 +121: `score` now persists its verdict beside the holdout and `is_calibrated` reads a recorded PASS instead of file existence — under the old rule the 2026-09-01 FAIL made the judge MORE trusted
gtm_core/gating.py 632
gtm_core/gtm_distill.py 609
gtm_core/hook_score.py 708
gtm_core/hooks.py 863
gtm_core/knowledge_meta.py 552
gtm_core/metering.py 531
gtm_core/outcomes.py 625  # 2026-09-03 +3: OPTOUT_OUTCOMES so the ledger knows who said stop
gtm_core/preflight_report.py 655
gtm_core/render_engines.py 538
gtm_core/render_manifest.py 680  # 2026-09-03 +65 for _fill_identity_fields: the loader raised TypeError on every multi-shot manifest the video-render skill actually writes, so `render_manifest validate` — the step the skill body mandates — could not run at all, and the cost gate it now also enforces was unreachable on the only path anyone takes
gtm_core/signal_record.py 639  # 2026-09-03 +4: JUDGE_COLUMNS gains judge_defect_class (the normalised routing key) plus the comment on why judge_verdict_reason cannot serve as one
gtm_core/spoiler_check.py 541
gtm_core/video_preflight.py 875
gtm_core/vo_timings.py 524
gtm_core/voc/capture.py 576
gtm_core/voc/collect.py 653
plugin/skills/account-dossier/scripts/render_dossier_pydocx.py 729
plugin/skills/account-plan/scripts/render_account_plan_pydocx.py 642
tests/linter/content_linter.py 833
tests/linter/merge_render_linter.py 1869  # 2026-09-04 +2: register `seat-stakes-missing` and `offer-does-their-work` so the summary groups them like every other rule
tests/linter/outreach_pack_linter.py 2783  # 2026-09-05 +132: `problem-asserts-internals` and `offer-not-a-solution-overview` — the operator stated both points three times and the copy never carried them, because voice.md rules 9 and 11 had no check. Rule 10 (agent as grammatical subject) quietly pulled every body back to asserting an architecture inferred from a website; both new rules convicted 6 of 6 packs before they passed them. Also narrows the 2026-09-04 external-anchor exemption, which had re-admitted the audit-shaped offer. Prior 2651 for 2026-09-04 +29: the `Would it be helpful if` offer shape (the sender retired both 'happy to' and 'I can'), giving verbs excluded in PAST tense too — 'would it be helpful if I SENT you the one-pager' was an artifact gate wearing the help-offer's clothes — and an external-anchor exemption on `offer-does-their-work`, because 'map your dispatch path against IMDA §2.1.2' is asymmetric knowledge while 'look at your dispatch path' is unpaid labour. Same day, 2622 for 2026-09-04 +83 for `capability-unargued`. `hook_coverage` has checked the SPREAD of capability declarations across a campaign since 2026-08-24 and never once checked that a body argues its own: three of six packs declared identity / protocol-proxy / security-policy and all three argued OBSERVABILITY — 'can the buyer check the record afterwards'. Evidence language is about the past, so the batch read as audit logging rather than runtime governance, which is what the operator meant by 'not even sure it has anything to do with agentic runtime governance'. The rule fails a body carrying none of its declared group's vocabulary, and a body that names its group once while spending the paragraph on evidence. Same day, 2539 for 2026-09-04 +79 for the two rules that close the gap between voice.md and what actually shipped. `seat-stakes-missing`: the persona-axis table has said since 2026-07 that a founder loses sleep over the stalled deal, not attribution — and `persona-lead-mismatch` could never enforce it, because it is fail-open by construction (it fires only on BORROWED vocabulary, and a mechanism-altitude body borrows nothing). Six emails to founders led on attribution mechanics and passed every gate. `offer-does-their-work`: an offer to go and look inside their own build is worth less than an hour of their own engineer and presumes they had not looked; the asymmetry we hold is cross-market. Same day, 2460 for 2026-09-04 +6 for the `I can <verb>` offer shape: "happy to" was retired at the sender's instruction, and without this the only CTA frames the gate accepted were the interrogative offer he had already rejected and the one he had just banned. Same day, 2454 for 2026-09-04 +19: the gate was MANDATING the phrasing the operator rejected as AI slop. `hedge-missing` accepted "tell me if this is already handled" and nothing plainer, so rotating that one sentence four ways to dodge repetition was the compliant path; `cta-question` required a question mark, forcing the interrogative-offer frame that ran in 6 of 6 rejected drafts; and the sign-off strip took one line, so a "Regards" above the name became the body's last sentence and failed every body that signed off the way he actually does. Earlier the same day, 2435 for 2026-09-04 +31 for `_is_person_name`: the old one-line `_NAME_RE` had NO SPACE in it, so every romanised Chinese, Malay or Indian given name written as two tokens — Hui Jie, Wei Ming, Siti Nurhaliza — was rejected with "is not a person's name". On a Singapore campaign that is not an edge case; the rule refused the market the run targeted. The replacement checks the placeholder vocabulary PER TOKEN so "name unconfirmed" is still refused whole. Earlier the same day, 2404 for 2026-09-04 +18 so a pack can be JUDGED: `_PACK_HEADING_RE` accepts both authored heading shapes (the old single-shape regex yielded company "" on all six packs of the sg-builders campaign — missing judge context reported as an error by nothing), and `**Contact:**` makes the sendable address a field instead of prose in the notes, which is the adjudication record's join key. Earlier the same day, 2400 for credit-is-verdict + cta-omits-gap + the help-offer exemption: three rules voice.md had stated in prose since 2026-08-19 with nothing enforcing them; all five packs of the sg-builders campaign passed every gate at zero errors while grading the reader in the opener
tests/linter/partnership_brief_linter.py 540
