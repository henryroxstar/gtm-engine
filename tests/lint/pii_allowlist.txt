# PII-check allowlist — the SINGLE source of truth for "this address identifies nobody".
#
# Consumed by BOTH:
#   - tests/lint/pii_check.py        (pre-commit + CI: blocks a real contact at commit time)
#   - scripts/oss-export.sh          (release gate: blocks a real contact reaching the public cut)
# Keeping one file avoids the drift footgun that has bitten the SHIPPING_DOCS /
# EXPECT_SKILLS_PUBLIC mirrors repeatedly — never copy these values into a second list.
#
# WHY THIS EXISTS. Prospecting features are written against real, messy data, so the
# tempting fixture is the row that actually broke the parser — a real person at a real
# company, pulled from a live RocketReach/Apollo run. That shipped to the public repo in
# v0.8.0 (see CHANGELOG 0.9.1). The de-brand token lint cannot catch it: the tenant's name
# is absent, so every gate passes. Hence a rule about OTHER people's identities.
#
# THE RULE. An email address in the shippable source surface must be provably non-real:
#   - an RFC-2606 reserved domain  (*.example, *.invalid, *.test, *.localhost, example.com)
#   - a single-letter placeholder  (a@x.com, b@y.com)
#   - a fictional domain declared below
#   - a full address declared below
# Anything else is treated as a real person until a human says otherwise.
#
# ADDING AN ENTRY. Prefer inventing a new .example domain over allowlisting a real one.
# Only add a real domain when it is a vendor/protocol shape with no person attached
# (an API host, a BCC ingest address), and say why on the comment line above it.
# NEVER add a free-mail domain wholesale — a real prospect's gmail is exactly what this
# gate exists to catch; pin the specific fixture address instead.

[fictional-domains]
# Generic placeholders already used across the suite.
acme
acmebank
acmerobotics
nimbuslabs
widget
company
yourcompany
work
blocked
allowed
evil
nowhere
dossiered
nodossier
tierbco
compiler
analytical
# Fictional university, for the one shape .example can't stand in for: the
# academic-domain gate (gtm_core.account_integrity) matches on a real `.edu`/`.ac.xx`
# suffix, so its test fixtures need a domain that actually carries one.
riverbend
# Fictional companies introduced when real prospects were removed (v0.9.1).
summitline
brightpath
forgeworks
eastvale
medipath
cascade
devtrial
wavelet
wideloop
seniorpath
datacore
relay
# Fictional vendors replacing the tenant's real competitive watchlist (v0.9.1).
northwind
halyard
cirrus
# Legacy fictional fixtures on a real TLD, admitted by name when the bare-domain rule
# arrived (2026-09-03). New fixtures go on .example instead of growing this list.
bigbank
betahealth
deltabank
softco
noloc
latestonly
difference
zenith
acmellc
queuedone
queuedtwo
tabulator
other
nexus
quorum
anvil
q3

[known-good-addresses]
# Placeholder-person LinkedIn slugs. A personal LinkedIn URL is otherwise always a finding —
# there is no "reserved" form of one — so the standard stand-in names are pinned here.
linkedin.com/in/jane-doe
linkedin.com/in/janedoe
linkedin.com/in/jane
linkedin.com/in/john-doe
linkedin.com/in/johndoe
linkedin.com/in/john
linkedin.com/in/someone
linkedin.com/in/your-profile
linkedin.com/in/firstname-lastname
linkedin.com/in/your-handle
# Publish identity — the maintainer, published on purpose.
register.henry@gmail.com
# Vendor/protocol shapes: an API host or ingest address, no person attached.
noreply@
@anthropic.com
@apollo.io
# Sentry DSNs are host-shaped ("public@<org>.ingest.sentry.io"), not addressed to anyone.
sentry.io
bcc.hubspot.com
git@github.com
# Documentation placeholders that read as addresses.
first.last@
@domain.com
exec@mail.other.com
# The one free-mail RULE-INPUT fixture: tests/test_merge_hygiene.py asserts the
# "email-freemail" rule fires, which needs a literal free-mail domain. Pinned as a full
# address so a genuine prospect's gmail still fails the gate.
chris@gmail.com

[fixture-url-domains]
# URL hosts allowed inside tests/ (suffix match: an entry covers its subdomains).
# The rule this feeds exists because a real practitioner's blog domain — carrying their
# verbatim published words — shipped as a parser fixture on 2026-08-11: no email, no
# tenant token, so every other gate passed it. Inside tests/, any http(s) host must be
# RFC-2606-reserved, a declared fictional domain, or listed here.
# ADDING AN ENTRY: only vendor APIs, public infrastructure, or standards bodies — shapes
# with no person attached. A blog, newsletter, or personal site is exactly what this
# section must never hold; fictionalize the fixture onto .example instead.
# Vendor APIs the code under test actually calls (mocked in tests, never fetched):
anthropic.com
apollo.io
calendly.com
deepseek.com
rocketreach.co
vibeprospecting.ai
explorium.ai
s3.amazonaws.com
googleapis.com
# Higgsfield's result CDN — vendor infrastructure, no person attached (gtm_core/media_fetch.py's
# host allowlist; fixtures use it in tests/test_media_fetch.py).
cloudfront.net
# Reap's user-upload S3 bucket (and the sibling-bucket NEGATIVE fixture asserting a pinned
# allowlist does not admit the provider's whole namespace). Vendor storage infrastructure, no
# person attached — gtm_core/reap_upload.py's ALLOWED_UPLOAD_HOSTS, tests/test_reap_upload.py.
amazonaws.com
# The bare-domain rule (2026-09-03) reads code surfaces outside tests/ too, so the vendor
# endpoints the engine actually calls, the public bodies and infrastructure it names as rule
# inputs, and the benchmark publishers a dashboard cites as data sources all belong here.
# Same bar as above: a company or an institution, never a person's site.
# Connected media / outreach / listening vendors (endpoints in gtm_core, agent, cockpit):
heygen.ai
higgsfield.ai
elevenlabs.io
saleshandy.com
syften.com
firecrawl.dev
sentry.io
hubspot.com
telegram.org
claude.ai
# Public infrastructure, package indexes, standards bodies, documentation sites:
github.com
githubusercontent.com
linkedin.com
google.com
google.dev
bing.com
duckduckgo.com
json-schema.org
sli.dev
azurewebsites.net
npmjs.org
pypi.org
crates.io
jsdelivr.net
luma.com
businesswire.com
# Public bodies named as regulators / sources (rule inputs and fixtures, no person):
sec.gov
treasury.gov
ftc.gov
gov.uk
gov.sg
gov.au
# Cold-email benchmark publishers a dashboard cites as its data sources (companies, with
# the citation URL beside the figure) — gtm_core/email_campaign_dashboard/config.py.
belkins.io
woodpecker.co
cleanlist.ai
# Free-mail providers as RULE INPUTS (the free-mail list in gtm_core/merge_hygiene/email.py
# and the linters). This admits the bare domain only; an ADDRESS on any of them is still
# judged by the email rule above, which never allowlists a free-mail domain wholesale.
gmail.com
yahoo.com
hotmail.com
outlook.com
icloud.com
aol.com
gmx.com
protonmail.com
mail.com
qq.com
163.com
yandex.com
# Search engines, as the NEGATIVE fixture for gtm_core.signal_record's
# `signal-source-is-search` rule: a results page is where research went looking, not the
# source that carries the fact, and it returns something different next month. A search
# host has no person attached; the fixtures assert the URL is REJECTED.
google.com
bing.com
duckduckgo.com
# Public infrastructure / standards bodies / platform hosts used as inert URL shapes:
github.com
githubusercontent.com
json-schema.org
sec.gov
linkedin.com
lnkd.in
# Provider CDN hosts pinned in gtm_core/media_fetch.py's egress allowlist. They appear in
# tests/test_media_fetch.py as the positive and negative controls for that allowlist — a
# vendor's own infrastructure, identifying no person. resource2 is listed because the test
# asserts it is REFUSED (the negative control that distinguishes a per-host pin from a
# *.heygen.ai wildcard), so it must stay spellable in the fixture.
heygen.ai

[third-party-allowed]
# Names the derived third-party roster (tests/lint/third_party_roster.py) must NOT treat
# as a leak. An entry here is a claim that the name identifies no customer relationship
# when it appears in shipped text — not merely that it is noisy.
#
# Two admissible reasons, and no others:
#   1. VENDOR / ECOSYSTEM TOOL. We integrate with it or cite it as a tool, so its name in
#      a field map, a connector or a comparison is product documentation. That it is ALSO
#      an account in content/ is a coincidence of the roster's source, not a disclosure.
#   2. PUBLIC INSTITUTION. A regulator, standards body or index whose rules the product
#      implements. Naming it discloses nothing about who we sell to.
#
# NOT admissible: "this is a big public company", or "the gate is noisy here". A real
# prospect stays out of shipped text however famous it is — the leak is the sentence
# around the name ("<company> sits on the AARM working group"), which is our research.
# Adding an entry needs a human and a reason on the line above it.

# --- vendors and ecosystem tools we integrate with or cite as tools ---
# CRM/sequencer export targets: named in field maps and connector docs.
hubspot
# Agent frameworks named in ICP/positioning prose as the ecosystem being sold into.
crewai
langchain
llamaindex
# Video/demo hosting used by the demo-capture and clip skills.
vimeo
# Data/AI platforms cited as integration or event-sourcing examples.
databricks
# Identity vendor cited in the account-integrity fixtures as an auth provider.
okta
# Market-data source cited in infographic prompt recipes.
bloomberg

# --- public institutions whose rules the product implements ---
# Singapore's financial regulator: named in signal-classification comments because its
# rulemaking is the signal being classified, not a customer relationship.
monetary authority of singapore

# --- leading tokens that are geography or a common noun, not an identity ---
# These are the FIRST word of a multi-word account name ("<city> <bank>", "<plural noun>
# <suffix>"). The account itself stays covered by its full-phrase key; only the bare
# leading token is released, because it appears in country maps, scoring-weight comments
# and ordinary prose. The system dictionary misses them because it is a 1934 wordlist
# with no plurals and no British spellings.
singapore
malaysia
london
dubai
emirates
cambridge
zurich
qatar
nottingham
nebraska
stanford
changi
weights
centre
automation
customers
retrofit

# --- AI-product keywords the signal detector looks for in NEWS ---
# These sit in merge_hygiene's trigger vocabulary alongside "agentforce" and "bedrock":
# the detector's job is to notice these products being launched, so the name is market
# vocabulary. That one is also an account in content/ is a coincidence of the roster's
# source, not a disclosure about who we sell to.
aidoc

# --- four-letter tokens that are ordinary words, not identities ---
# Released because the four-character floor (see third_party_roster.MIN_TOKEN_LEN) admits
# them: each is a common noun or a technical term in this codebase that happens to also be
# the start of an account name. The accounts stay covered by their full-phrase keys.
luma
hang
guys
cebu
