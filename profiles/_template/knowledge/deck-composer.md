# Deck composer notes — <Company>

> Template placeholder — replace with your own. Per-company deck composition preferences (section order, slide density, recurring proof points) consumed by `build-deck`.
